<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="zh-CN"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://xuyuanzhe.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://xuyuanzhe.github.io/" rel="alternate" type="text/html" hreflang="zh-CN"/><updated>2026-05-09T09:26:45+00:00</updated><id>https://xuyuanzhe.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Hermes 的一些资源文档</title><link href="https://xuyuanzhe.github.io/blog/2026/hermes-doc/" rel="alternate" type="text/html" title="Hermes 的一些资源文档"/><published>2026-05-07T15:00:00+00:00</published><updated>2026-05-07T15:00:00+00:00</updated><id>https://xuyuanzhe.github.io/blog/2026/hermes-doc</id><content type="html" xml:base="https://xuyuanzhe.github.io/blog/2026/hermes-doc/"><![CDATA[<h1 id="hermes-agent-使用能力提升指南">Hermes Agent 使用能力提升指南</h1> <h2 id="一hermes-agent-定位与能力框架">一、Hermes Agent 定位与能力框架</h2> <p>Hermes Agent 是 NousResearch 基于 LLaMA 架构打造的<strong>轻量级开源 AI 代理框架</strong>，核心优势是「技能模块化」「配置灵活性」「社区生态活跃」。装完基础环境后，提升能力的核心在于<strong>从「工具使用」到「生态融合」，再到「工作流程自动化」的闭环优化</strong>。</p> <hr/> <h2 id="二高效学习与资源利用保姆级网址分类">二、高效学习与资源利用（保姆级网址分类）</h2> <h3 id="一官方生态权威标准与技术源码">（一）官方生态：权威标准与技术源码</h3> <table> <thead> <tr> <th>资源类型</th> <th>网址</th> <th>专家注解</th> </tr> </thead> <tbody> <tr> <td>官方文档</td> <td>http://hermes-agent.nousresearch.com/docs</td> <td><strong>必看核心！</strong>重点读「技能开发规范」「网关配置」「会话管理」，是定制化的基础。</td> </tr> <tr> <td>GitHub 主仓库</td> <td>http://github.com/NousResearch/hermes-agent</td> <td>关注 <code class="language-plaintext highlighter-rouge">dev</code> 分支的更新，优先用 Docker 部署避免环境冲突；<strong>Issues 区是 bug 反馈的最佳渠道</strong>。</td> </tr> <tr> <td>Skills 市场</td> <td>http://agentskills.io</td> <td>筛选原则：技能下载量&gt;1000、更新时间&lt;1 月、标签包含「生产级」；重点关注自动化办公、数据分析类技能。</td> </tr> </tbody> </table> <h3 id="二中文资源快速上手与本地适配">（二）中文资源：快速上手与本地适配</h3> <table> <thead> <tr> <th>资源类型</th> <th>网址</th> <th>专家注解</th> </tr> </thead> <tbody> <tr> <td>中文文档</td> <td>http://hermes.xaapi.ai</td> <td>本地化翻译质量高，适合国内用户快速理解「配置流程」「常用命令」。</td> </tr> <tr> <td>中文社区 FAQ</td> <td>https://hermesagent.org.cn/docs/reference/faq</td> <td><strong>问题排查首选！</strong>覆盖 90% 常见报错，如「模型加载失败」「网关连接超时」。</td> </tr> <tr> <td>Discord 社区</td> <td>http://discord.gg/nousresearch</td> <td>国内用户建议用中文频道交流，管理员和核心开发者会定期解答问题；<strong>每周二有「技能开发直播」</strong>。</td> </tr> </tbody> </table> <h3 id="三进阶学习从入门到精通">（三）进阶学习：从入门到精通</h3> <table> <thead> <tr> <th>资源类型</th> <th>网址</th> <th>专家注解</th> </tr> </thead> <tbody> <tr> <td>Hermes 橙皮书</td> <td>https://huasheng.ai/orange-books/hermes-agent/</td> <td>体系化学习教材，从「架构设计」到「实战案例」，适合想深入的用户。</td> </tr> <tr> <td>进阶使用技巧</td> <td>https://x.com/LufzzLiz/status/2042237123865297267?s=20</td> <td>「上手后必做的十件事」，覆盖技能组合、配置优化、自动化脚本等，是提升效率的捷径。</td> </tr> </tbody> </table> <hr/> <h2 id="三实战化操作命令专家级分类与场景">三、实战化操作命令（专家级分类与场景）</h2> <h3 id="一基础操作快速启动与交互">（一）基础操作：快速启动与交互</h3> <table> <thead> <tr> <th>命令</th> <th>场景与建议</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">Hermes</code></td> <td>标准启动方式，自动加载默认配置；首次启动建议先看 <code class="language-plaintext highlighter-rouge">hermes status</code> 检查状态。</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">Hermes -c</code></td> <td>继续上次对话，<strong>适合多轮任务</strong>，如写代码、分析文档。</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">hermes -q "问题"</code></td> <td>单次问答模式，<strong>适合查询类任务</strong>，如「Python 列表去重方法」。</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">hermes --help</code></td> <td><strong>命令索引</strong>，忘记某个命令参数时的救星。</td> </tr> </tbody> </table> <h3 id="二配置与优化提升性能">（二）配置与优化：提升性能</h3> <table> <thead> <tr> <th>命令</th> <th>场景与建议</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">hermes setup</code></td> <td>完整配置向导，首次安装必跑，会检测依赖、配置模型、设置消息平台。</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">hermes model</code></td> <td>模型选择界面，建议优先用 LLaMA-3-8B，平衡性能和速度；GPU 显存&gt;16GB 可尝试 70B。</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">hermes config</code></td> <td>查看当前配置，重点关注 <code class="language-plaintext highlighter-rouge">model_path</code>（模型路径）、<code class="language-plaintext highlighter-rouge">skills_dir</code>（技能目录）。</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">hermes config set KEY VAL</code></td> <td>手动修改配置，如 <code class="language-plaintext highlighter-rouge">hermes config set model.gpu_layers 40</code>（CPU 显存&gt;32GB 可设为 0）。</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">hermes doctor</code></td> <td><strong>健康检查</strong>，建议每周跑一次，会检测依赖缺失、模型损坏、网关配置问题。</td> </tr> </tbody> </table> <h3 id="三技能管理生态利用">（三）技能管理：生态利用</h3> <table> <thead> <tr> <th>命令</th> <th>场景与建议</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">hermes skills search 关键词</code></td> <td>技能搜索，如 <code class="language-plaintext highlighter-rouge">hermes skills search 数据分析</code>；注意筛选「verified」标签的技能。</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">hermes skills install ID</code></td> <td>技能安装，ID 可从 AgentsSkills 市场获取；安装后会自动添加到技能目录。</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">hermes skills list</code></td> <td>查看已装技能，重点看「active」状态（是否启用）、”version”（是否需要更新）。</td> </tr> </tbody> </table> <h3 id="四网关与消息平台多端协同">（四）网关与消息平台：多端协同</h3> <table> <thead> <tr> <th>命令</th> <th>场景与建议</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">hermes gateway setup</code></td> <td>配置消息平台，支持 Discord、Telegram、Slack；建议先用 Discord 测试。</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">hermes gateway start</code></td> <td>启动网关，会在后台运行；建议用 <code class="language-plaintext highlighter-rouge">nohup</code> 命令保持运行：<code class="language-plaintext highlighter-rouge">nohup hermes gateway start &gt; gateway.log 2&gt;&amp;1 &amp;</code>。</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">hermes gateway status</code></td> <td>查看网关状态，重点看「connections」（连接数）、”messages”（消息统计）。</td> </tr> </tbody> </table> <h3 id="五系统维护长期稳定运行">（五）系统维护：长期稳定运行</h3> <table> <thead> <tr> <th>命令</th> <th>场景与建议</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">hermes update</code></td> <td>更新到最新版，建议每周更新一次；更新前先备份 <code class="language-plaintext highlighter-rouge">config.yaml</code> 和技能目录。</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">hermes memory stats</code></td> <td>查看记忆统计，重点看「session_count」（会话数）、”token_count”（token 消耗）。</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">hermes memory prune</code></td> <td>清理记忆，可删除过期会话；建议保留最近 30 天的重要会话。</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">hermes sessions list</code></td> <td>查看会话历史，可根据会话 ID 恢复对话；会话文件默认存放在 <code class="language-plaintext highlighter-rouge">~/.hermes/sessions/</code>。</td> </tr> </tbody> </table> <h3 id="六进阶功能自动化与定制化">（六）进阶功能：自动化与定制化</h3> <table> <thead> <tr> <th>命令</th> <th>场景与建议</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">hermes memory prune --days 7</code></td> <td>自动清理 7 天前的会话，适合定期维护脚本。</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">hermes skills install --github &lt;repo&gt;</code></td> <td>安装 GitHub 上的自定义技能，如 <code class="language-plaintext highlighter-rouge">hermes skills install --github username/repo</code>。</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">hermes model --list</code></td> <td>列出本地可用模型，适合对比不同模型的性能。</td> </tr> </tbody> </table> <hr/> <h2 id="四专家视角的能力提升路径">四、专家视角的能力提升路径</h2> <ol> <li> <p><strong>基础阶段（1-2 周）</strong>：完成 <code class="language-plaintext highlighter-rouge">hermes setup</code>，熟悉常用命令，安装 3-5 个核心技能（如「网页解析」「代码生成」「文档翻译」）。</p> </li> <li> <p><strong>进阶阶段（2-4 周）</strong>：学习技能开发规范，尝试写简单的自定义技能；配置 Discord 网关，实现多端协同。</p> </li> <li> <p><strong>专业阶段（4 周+）</strong>：结合 Docker 部署，实现生产级环境；开发自动化脚本，如「每日新闻汇总」「代码质量检查」；参与社区贡献，提交技能或 bug 修复。</p> </li> </ol> <hr/> <h2 id="五常见误区与避坑指南">五、常见误区与避坑指南</h2> <ol> <li> <p><strong>不要盲目装技能</strong>：技能越多，启动时间越长，内存消耗越大；建议只装常用的 5-10 个技能。</p> </li> <li> <p><strong>不要忽略健康检查</strong>：<code class="language-plaintext highlighter-rouge">hermes doctor</code> 会检测到很多隐性问题，如依赖过期、模型损坏，定期跑可以避免突发故障。</p> </li> <li> <p><strong>不要直接修改 config.yaml</strong>：手动修改配置文件容易出错，建议用 <code class="language-plaintext highlighter-rouge">hermes config set</code> 命令。</p> </li> <li> <p><strong>不要忽视网关安全</strong>：网关启动后会暴露在公网，建议设置密码或限制 IP 访问。</p> </li> </ol>]]></content><author><name></name></author><category term="技术"/><category term="AI"/><summary type="html"><![CDATA[Some resource documents of Hermes]]></summary></entry><entry><title type="html">macOS 系统安装赫尔墨斯</title><link href="https://xuyuanzhe.github.io/blog/2026/Hermes-macOS-Intel/" rel="alternate" type="text/html" title="macOS 系统安装赫尔墨斯"/><published>2026-04-16T15:00:00+00:00</published><updated>2026-04-16T15:00:00+00:00</updated><id>https://xuyuanzhe.github.io/blog/2026/Hermes-macOS-Intel</id><content type="html" xml:base="https://xuyuanzhe.github.io/blog/2026/Hermes-macOS-Intel/"><![CDATA[<h1 id="hermes--web-ui-本地部署指南macos-intel">Hermes + Web UI 本地部署指南（macOS Intel）</h1> <p>本文档指导你在 macOS Intel 笔记本上完成 <strong>Hermes Agent</strong>（NousResearch 出品的 AI Agent 运行时）和 <strong>hermes-web-ui</strong>（可视化管理界面）的完整部署。</p> <hr/> <h2 id="一整体架构">一、整体架构</h2> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>浏览器 ──▶ hermes-web-ui (BFF, 端口 8648) ──▶ Hermes Gateway (端口 8642)
                  │
             Hermes CLI
                  │
          ~/.hermes/config.yaml
          ~/.hermes/auth.json
</code></pre></div></div> <ul> <li><strong>Hermes Agent</strong>：核心后端，提供 CLI 和 Gateway 服务，连接各种 LLM 提供商</li> <li><strong>hermes-web-ui</strong>：基于 Vue 3 的 Web 管理界面，通过 BFF 层与 Hermes 通信</li> </ul> <hr/> <h2 id="二前置条件">二、前置条件</h2> <table> <thead> <tr> <th>依赖</th> <th>说明</th> <th>检查命令</th> </tr> </thead> <tbody> <tr> <td>macOS (Intel)</td> <td>系统要求</td> <td><code class="language-plaintext highlighter-rouge">uname -m</code> 应输出 <code class="language-plaintext highlighter-rouge">x86_64</code></td> </tr> <tr> <td>Python 3</td> <td>Hermes Agent 依赖（安装脚本会自动处理）</td> <td><code class="language-plaintext highlighter-rouge">python3 --version</code></td> </tr> <tr> <td>Node.js ≥ 18</td> <td>hermes-web-ui 依赖</td> <td><code class="language-plaintext highlighter-rouge">node --version</code></td> </tr> <tr> <td>LLM API Key</td> <td>至少一个（OpenAI / OpenRouter / Nous Portal 等）</td> <td>—</td> </tr> </tbody> </table> <blockquote> <p>如果还没有 Node.js，推荐用 Homebrew 安装：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>brew <span class="nb">install </span>node
</code></pre></div> </div> </blockquote> <hr/> <h2 id="三第一步安装-hermes-agent">三、第一步：安装 Hermes Agent</h2> <h3 id="31-一键安装">3.1 一键安装</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl <span class="nt">-fsSL</span> https://raw.githubusercontent.com/NousResearch/hermes-agent/main/scripts/install.sh | bash
</code></pre></div></div> <p>安装完成后，重新加载 shell 配置：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">source</span> ~/.zshrc
</code></pre></div></div> <h3 id="32-验证安装">3.2 验证安装</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>hermes <span class="nt">--version</span>
</code></pre></div></div> <p>如果能正常输出版本号，说明安装成功。</p> <h3 id="33-初始配置">3.3 初始配置</h3> <p>运行配置向导：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>hermes setup
</code></pre></div></div> <p>或者分步配置：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 选择 LLM 提供商和模型</span>
hermes model

<span class="c"># 查看/配置可用工具</span>
hermes tools
</code></pre></div></div> <p>配置过程中需要输入你的 LLM API Key（如 OpenAI API Key），配置会保存到 <code class="language-plaintext highlighter-rouge">~/.hermes/auth.json</code>。</p> <h3 id="34-测试-hermes-能否正常工作">3.4 测试 Hermes 能否正常工作</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>hermes
</code></pre></div></div> <p>进入对话界面，发送一条消息确认 LLM 连接正常。确认无误后按 <code class="language-plaintext highlighter-rouge">Ctrl+C</code> 退出。</p> <h3 id="35-启动-gateway-服务">3.5 启动 Gateway 服务</h3> <p>hermes-web-ui 需要连接 Hermes Gateway，因此要先启动它：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>hermes gateway
</code></pre></div></div> <p>Gateway 默认监听 <code class="language-plaintext highlighter-rouge">localhost:8642</code>。保持此终端运行，或使用后台方式运行。</p> <hr/> <h2 id="四第二步安装-hermes-web-ui">四、第二步：安装 hermes-web-ui</h2> <p>提供三种安装方式，推荐方式 A。</p> <h3 id="方式-anpm-全局安装推荐">方式 A：npm 全局安装（推荐）</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>npm <span class="nb">install</span> <span class="nt">-g</span> hermes-web-ui
</code></pre></div></div> <p>启动服务：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>hermes-web-ui start
</code></pre></div></div> <h3 id="方式-b一键脚本安装">方式 B：一键脚本安装</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>bash &lt;<span class="o">(</span>curl <span class="nt">-fsSL</span> https://raw.githubusercontent.com/EKKOLearnAI/hermes-web-ui/main/scripts/setup.sh<span class="o">)</span>
</code></pre></div></div> <p>此脚本会自动检测并安装缺失的 Node.js。</p> <h3 id="方式-c从源码构建适合开发者">方式 C：从源码构建（适合开发者）</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone https://github.com/EKKOLearnAI/hermes-web-ui.git
<span class="nb">cd </span>hermes-web-ui
npm <span class="nb">install
</span>npm run dev
</code></pre></div></div> <p>源码模式下前端运行在 <code class="language-plaintext highlighter-rouge">http://localhost:5173</code>，BFF 运行在 <code class="language-plaintext highlighter-rouge">http://localhost:8648</code>。</p> <hr/> <h2 id="五访问-web-ui">五、访问 Web UI</h2> <p>安装并启动后，打开浏览器访问：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>http://localhost:8648
</code></pre></div></div> <p>首次启动时，系统会自动生成一个认证 Token，在终端输出中可以看到。复制该 Token 输入即可登录。</p> <hr/> <h2 id="六hermes-web-ui-常用命令">六、hermes-web-ui 常用命令</h2> <table> <thead> <tr> <th>命令</th> <th>说明</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">hermes-web-ui start</code></td> <td>以守护进程模式启动</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">hermes-web-ui start --port 9000</code></td> <td>指定自定义端口启动</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">hermes-web-ui stop</code></td> <td>停止服务</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">hermes-web-ui restart</code></td> <td>重启服务</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">hermes-web-ui status</code></td> <td>查看运行状态</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">hermes-web-ui update</code></td> <td>更新到最新版本并重启</td> </tr> </tbody> </table> <hr/> <h2 id="七认证配置可选">七、认证配置（可选）</h2> <p>hermes-web-ui 支持三种认证方式：</p> <ol> <li> <p><strong>Token 认证（默认）</strong>：首次启动自动生成，也可通过环境变量手动指定：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">AUTH_TOKEN</span><span class="o">=</span>your-custom-token hermes-web-ui start
</code></pre></div> </div> </li> <li> <p><strong>用户名密码</strong>：在 Web UI 的设置页面中配置。</p> </li> <li> <p><strong>禁用认证</strong>（仅限本地开发环境）：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">AUTH_DISABLED</span><span class="o">=</span>1 hermes-web-ui start
</code></pre></div> </div> </li> </ol> <hr/> <h2 id="八日常使用流程">八、日常使用流程</h2> <p>每次使用时，确保以下两个服务都在运行：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 终端 1：启动 Hermes Gateway</span>
hermes gateway

<span class="c"># 终端 2：启动 Web UI（如果未在守护模式运行）</span>
hermes-web-ui start
</code></pre></div></div> <p>然后浏览器打开 <code class="language-plaintext highlighter-rouge">http://localhost:8648</code> 即可。</p> <hr/> <h2 id="九常见问题">九、常见问题</h2> <h3 id="q-hermes-command-not-found">Q: <code class="language-plaintext highlighter-rouge">hermes: command not found</code></h3> <p>安装后未重新加载 shell 配置。执行 <code class="language-plaintext highlighter-rouge">source ~/.zshrc</code> 后重试。</p> <h3 id="q-hermes-web-ui-无法连接-hermes">Q: hermes-web-ui 无法连接 Hermes</h3> <p>确认 Hermes Gateway 是否在运行（<code class="language-plaintext highlighter-rouge">hermes gateway</code>），默认端口 8642 是否被占用。</p> <h3 id="q-npm-install-报权限错误">Q: npm install 报权限错误</h3> <p>不要使用 <code class="language-plaintext highlighter-rouge">sudo npm install -g</code>，改为修复 npm 全局目录权限：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir</span> <span class="nt">-p</span> ~/.npm-global
npm config <span class="nb">set </span>prefix <span class="s1">'~/.npm-global'</span>
<span class="nb">echo</span> <span class="s1">'export PATH=~/.npm-global/bin:$PATH'</span> <span class="o">&gt;&gt;</span> ~/.zshrc
<span class="nb">source</span> ~/.zshrc
</code></pre></div></div> <p>然后重新执行 <code class="language-plaintext highlighter-rouge">npm install -g hermes-web-ui</code>。</p> <h3 id="q-nodejs-版本过低">Q: Node.js 版本过低</h3> <p>hermes-web-ui 要求 Node.js ≥ 18。升级方式：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>brew upgrade node
</code></pre></div></div> <hr/> <h2 id="十卸载">十、卸载</h2> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 卸载 hermes-web-ui</span>
hermes-web-ui stop
npm uninstall <span class="nt">-g</span> hermes-web-ui

<span class="c"># 卸载 Hermes Agent（删除配置目录）</span>
<span class="nb">rm</span> <span class="nt">-rf</span> ~/.hermes
<span class="c"># 删除 hermes 可执行文件（路径参考安装时的输出）</span>
</code></pre></div></div> <hr/> <h2 id="参考链接">参考链接</h2> <ul> <li>Hermes Agent：https://github.com/NousResearch/hermes-agent</li> <li>hermes-web-ui：https://github.com/EKKOLearnAI/hermes-web-ui</li> </ul>]]></content><author><name></name></author><category term="技术"/><category term="AI"/><summary type="html"><![CDATA[Install Hermes Agent + hermes-web-ui on macOS Intel]]></summary></entry><entry><title type="html">OpenClaw 记忆系统源码拆解：从写入到晋升的全链路</title><link href="https://xuyuanzhe.github.io/blog/2026/openclaw-memory-system/" rel="alternate" type="text/html" title="OpenClaw 记忆系统源码拆解：从写入到晋升的全链路"/><published>2026-04-15T15:00:00+00:00</published><updated>2026-04-15T15:00:00+00:00</updated><id>https://xuyuanzhe.github.io/blog/2026/openclaw-memory-system</id><content type="html" xml:base="https://xuyuanzhe.github.io/blog/2026/openclaw-memory-system/"><![CDATA[<p>接着上一篇对 OpenClaw 整体架构的分析，这次单独拆一下它的记忆系统。</p> <p>上次提到过 OpenClaw 用 <code class="language-plaintext highlighter-rouge">MEMORY.md</code> + <code class="language-plaintext highlighter-rouge">memory/日期.md</code> 做双层记忆管理，但没有深入到实现细节。这次从源码层面把写入、晋升、召回的全链路走一遍，顺便分析一下这套系统的不确定性问题。</p> <h2 id="记忆系统全景">记忆系统全景</h2> <p>OpenClaw 的设计原则是：一切持久状态都是磁盘上的 Markdown 文件。</p> <p>完整的文件体系：</p> <table> <thead> <tr> <th>文件</th> <th>用途</th> <th>加载时机</th> </tr> </thead> <tbody> <tr> <td>AGENTS.md</td> <td>工作区规则、安全边界</td> <td>每次会话（最高优先级）</td> </tr> <tr> <td>SOUL.md</td> <td>Agent 个性、沟通风格</td> <td>每次会话</td> </tr> <tr> <td>USER.md</td> <td>用户档案</td> <td>每次会话</td> </tr> <tr> <td>MEMORY.md</td> <td>长期记忆（已验证事实、决策）</td> <td>仅主会话</td> </tr> <tr> <td>memory/YYYY-MM-DD.md</td> <td>日记忆（当天观察、临时笔记）</td> <td>当天 + 昨天自动加载</td> </tr> <tr> <td>DREAMS.md</td> <td>梦境日记（Dreaming 系统输出）</td> <td>不自动注入</td> </tr> </tbody> </table> <p>其中 <code class="language-plaintext highlighter-rouge">AGENTS.md</code>、<code class="language-plaintext highlighter-rouge">SOUL.md</code>、<code class="language-plaintext highlighter-rouge">USER.md</code> 定义的是身份和规则，每次会话启动时加载。而 <code class="language-plaintext highlighter-rouge">MEMORY.md</code> 和 <code class="language-plaintext highlighter-rouge">memory/YYYY-MM-DD.md</code> 是另一套机制——承载 Agent 在对话中积累的动态记忆，有专门的写入、演进和召回管线。</p> <h2 id="记忆写入两条路径">记忆写入：两条路径</h2> <h3 id="agent-主动写入">Agent 主动写入</h3> <p>最常用的路径。对话过程中，Agent 随时可以调用 <code class="language-plaintext highlighter-rouge">write</code> 工具写入记忆文件：</p> <ul> <li>用户显式要求：”记住我偏好 TypeScript”</li> <li>Agent 自主判断：认为某些信息值得保存，自行决定写入</li> </ul> <p>关键点在于：<strong>是否写入、写入什么、用什么格式，完全由 LLM 自主决定</strong>。没有结构化的提取规则，没有强制的写入模板。</p> <p>AGENTS.md 模板里对写入的指导是这样的：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Write It Down - No "Mental Notes"!
- Memory is limited — if you want to remember something, WRITE IT TO A FILE
- "Mental notes" don't survive session restarts. Files do.
- When someone says "remember this" → update memory/YYYY-MM-DD.md
</code></pre></div></div> <p>可以看到，这些是方向性的建议（”capture what matters”），而非结构化的提取规则。</p> <h3 id="memory-flush-自动写入">Memory Flush 自动写入</h3> <p>Memory Flush 是上下文压缩（Compaction）前的安全网——确保激进裁剪之前，重要信息已被保存。</p> <p>触发条件：</p> <ul> <li>Token 阈值：<code class="language-plaintext highlighter-rouge">softThresholdTokens</code>（默认 4000）— 距离 Compaction 的 token 距离</li> <li>文件大小阈值：<code class="language-plaintext highlighter-rouge">forceFlushTranscriptBytes</code>（默认 2MB）</li> </ul> <p>触发时，系统向 LLM 发送一段特殊指令：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Pre-compaction memory flush turn.
The session is near auto-compaction; capture durable memories to disk.
Store durable memories only in memory/YYYY-MM-DD.md.
Treat MEMORY.md, DREAMS.md, SOUL.md, TOOLS.md, AGENTS.md as read-only during this flush.
If nothing to store, reply with NO_REPLY.
</code></pre></div></div> <p>Flush 期间，<code class="language-plaintext highlighter-rouge">write</code> 工具被包装为仅追加模式（<code class="language-plaintext highlighter-rouge">appendOnly</code>），只允许写入当天的日记忆文件。</p> <blockquote> <h5 id="note">Note</h5> <p class="block-tip">两条路径都写入 <code class="language-plaintext highlighter-rouge">memory/YYYY-MM-DD.md</code>。主动写入是日常的主要方式，Memory Flush 是压缩前的”最后一次救济机会”。</p> </blockquote> <h2 id="日记忆到长期记忆的晋升">日记忆到长期记忆的晋升</h2> <h3 id="默认方式agent-自主整理">默认方式：Agent 自主整理</h3> <p>不启用 Dreaming 的情况下，晋升完全靠 Agent 自己判断：</p> <ol> <li>对话中直接写入 <code class="language-plaintext highlighter-rouge">MEMORY.md</code>（AGENTS.md 模板允许：”You can read, edit, and update MEMORY.md freely in main sessions”）</li> <li>心跳（Heartbeat）期间定期回顾日记忆并更新</li> </ol> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Memory Maintenance (During Heartbeats)
Periodically (every few days), use a heartbeat to:
1. Read through recent memory/YYYY-MM-DD.md files
2. Identify significant events, lessons, or insights worth keeping long-term
3. Update MEMORY.md with distilled learnings
4. Remove outdated info from MEMORY.md that's no longer relevant
</code></pre></div></div> <p>特点是灵活但不确定——是否执行、何时执行、整理质量如何，完全取决于 LLM。</p> <h3 id="dreaming-梦境系统三阶段异步演进">Dreaming 梦境系统：三阶段异步演进</h3> <p>这是 OpenClaw 比较有意思的设计。Dreaming 默认禁用（<code class="language-plaintext highlighter-rouge">DEFAULT_MEMORY_DREAMING_ENABLED = false</code>），启用后通过 Cron 定时任务运行（默认凌晨 3 点），将短期信号逐步转化为长期记忆。</p> <p>三个阶段按顺序执行：Light → REM → Deep。</p> <p><strong>浅睡眠（Light Sleep）— 摄取与去重</strong></p> <p>从多个信号源搜集候选记忆片段：</p> <ul> <li>日记忆文件：逐行提取（最小 8 字符，最大 280 字符，最多 4 行合并）</li> <li>会话转录：按 Agent 和 Session 聚合（每次最多 240 条）</li> <li>短期回忆存储：<code class="language-plaintext highlighter-rouge">memory/.dreams/short-term-recall.json</code></li> </ul> <p>去重使用 Jaccard 相似度（阈值 0.9）。这个阶段不调用 LLM，完全是确定性的文本处理。</p> <p><strong>快速眼动睡眠（REM Sleep）— 反射与候选真理</strong></p> <p>对候选信号做模式分析，识别反复出现的主题和高置信度的”候选真理”。</p> <p>主题强度计算：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>strength = min(1, (count / totalEntries) × 2)
</code></pre></div></div> <p>候选真理的置信度分数：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>confidence = avgScore × 0.45
           + recallStrength × 0.25
           + consolidation × 0.20
           + conceptual × 0.10

其中：
  recallStrength = min(1, log1p(recallCount) / log1p(6))
  consolidation  = min(1, recallDays.length / 3)
  conceptual     = min(1, conceptTags.length / 6)
</code></pre></div></div> <p>去重阈值提高到 Jaccard 0.88，仅保留置信度 &gt;= 0.45 的候选，最多 3 条。</p> <p><strong>深度睡眠（Deep Sleep）— 六维评分与晋升</strong></p> <p>这是决定一条记忆能否晋升为长期记忆的最终关口。六个加权信号：</p> <table> <thead> <tr> <th>信号</th> <th>权重</th> <th>计算方式</th> </tr> </thead> <tbody> <tr> <td>频率</td> <td>0.24</td> <td>min(1, ln(signalCount + 1) / ln(11))</td> </tr> <tr> <td>相关性</td> <td>0.30</td> <td>totalScore / max(1, signalCount)</td> </tr> <tr> <td>多样性</td> <td>0.15</td> <td>min(1, max(uniqueQueries, recallDays) / 5)</td> </tr> <tr> <td>时效性</td> <td>0.15</td> <td>exp(-λ × ageDays)，λ = ln(2)/14，半衰期 14 天</td> </tr> <tr> <td>巩固度</td> <td>0.10</td> <td>基于多日重现或 grounded 信号强度</td> </tr> <tr> <td>概念丰富度</td> <td>0.06</td> <td>min(1, conceptTags.length / 6)</td> </tr> </tbody> </table> <p>巩固度的计算取两个分支的最大值：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>// 分支 1：基于 recallDays 的时间跨度
spacing = min(1, ln(recallDays.length - 1) / ln(4))
span    = min(1, (maxDay - minDay) / 7天)
consolidation_a = 0.55 × spacing + 0.45 × span

// 分支 2：基于 grounded 信号计数
consolidation_b = min(1, groundedCount / 3)

consolidation = max(consolidation_a, consolidation_b)
</code></pre></div></div> <p>Light Sleep 和 REM Sleep 的命中记录还会额外加分：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>phaseBoost = 0.06 × lightStrength × lightRecency
           + 0.09 × remStrength × remRecency
// 衰减半衰期同为 14 天
</code></pre></div></div> <p>最终晋升门控，三个条件必须同时满足：</p> <ul> <li><code class="language-plaintext highlighter-rouge">score &gt;= 0.80</code></li> <li><code class="language-plaintext highlighter-rouge">totalSignalCount &gt;= 3</code>（recallCount + dailyCount + groundedCount）</li> <li><code class="language-plaintext highlighter-rouge">max(uniqueQueries, recallDays.length) &gt;= 3</code></li> </ul> <p>通过门控的候选会被重新水合（从日文件中重新读取，确保不写入过时内容），然后追加到 <code class="language-plaintext highlighter-rouge">MEMORY.md</code>。</p> <h2 id="记忆召回与反馈环">记忆召回与反馈环</h2> <p>持久化的记忆通过 <code class="language-plaintext highlighter-rouge">memory_search</code> 工具被召回：</p> <ul> <li>搜索后端：支持 builtin（SQLite + FTS，可选 sqlite-vec 向量扩展）和 QMD 两种；无 embedding 时自动降级为 FTS 全文索引</li> <li>信号记录：每次搜索返回结果后，后台异步调用 <code class="language-plaintext highlighter-rouge">recordShortTermRecalls</code>，把查询、路径、评分写入 <code class="language-plaintext highlighter-rouge">memory/.dreams/short-term-recall.json</code></li> <li>反馈环（仅 Dreaming 启用时）：召回信号被 Dreaming 消费，影响六维评分中的频率和相关性，形成”越被检索 → 越容易晋升”的正向循环</li> </ul> <h2 id="这套系统的不确定性问题">这套系统的不确定性问题</h2> <p>管线的设计理念是优秀的：异步演进、多阶段过滤、正向反馈。但从工程角度看，有几个不确定性环节值得注意。</p> <p><strong>写入完全依赖 LLM 判断</strong></p> <p>无论主动写入还是 Memory Flush，写什么、怎么写都由 LLM 单次推理决定。不同模型、不同上下文长度、甚至同一模型的不同推理轮次，写入内容可能完全不同。你告诉 Agent 自己的名字、城市和饮食偏好，它可能只记住了名字，漏掉其余两个。</p> <p><strong>Memory Flush 的盲区</strong></p> <p>Flush 只在上下文接近压缩阈值时触发。如果一次对话很短（没触发 Compaction），而 Agent 又没主动写入，对话中的信息就不会被持久化。它只能保证”长对话压缩前不丢”，不能保证”短对话也记住”。</p> <p><strong>晋升的不确定性</strong></p> <p>默认路径下，晋升完全靠 Agent 自主判断，可能长期不回顾。Dreaming 路径虽然有量化的评分机制，但也有三个问题：</p> <ol> <li><strong>Cron 周期延迟</strong>：晋升门控要求信号计数 &gt;= 3 且跨日数 &gt;= 3，一条记忆通常需要多次跨日积累。”我下周二飞杭州”这样的时效性信息，等信号积累完，飞机可能已经起飞了。</li> <li><strong>Jaccard 去重无法捕捉语义</strong>：”用户喜欢苹果”和”用户爱吃苹果”在语义上是同一件事，但 Jaccard 基于词汇重叠，可能判定它们不相似，导致同一事实存在多个版本。</li> <li><strong>统计评分而非语义重要性</strong>：Deep Sleep 评分完全基于统计信号（检索次数、出现天数），没有 LLM 参与语义判断。”我对花生过敏”只被提及一次但极其重要，在评分中可能远低于一条反复出现但不重要的信息。</li> </ol> <p>不确定性的完整链路：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>对话内容
  ↓ ❶ LLM 自主判断是否写入（可能遗漏）
  ↓ ❷ 短对话无 Flush 安全网（可能不触发）
日记忆文件
  ↓ ❸ 晋升不确定
  │   ├─ 默认路径：Agent 自主判断，可能不执行
  │   └─ Dreaming 路径（默认禁用）：
  │        Jaccard 机械去重（无语义理解）
  │        六维统计评分（无语义判断）
MEMORY.md
  ↓ ❹ 召回受限（无 embedding 时降级为词法匹配）
对话上下文
</code></pre></div></div> <blockquote> <h5 id="note-1">Note</h5> <p class="block-tip">这些不确定性不算设计缺陷——通用框架要在提取精度和系统复杂度之间做权衡。但对于需要精确记住用户事实的场景，从写入到晋升到召回的不确定性叠加起来，确实会影响体验。</p> </blockquote> <h2 id="可以优化的方向">可以优化的方向</h2> <p>分析完这套系统的不确定性，自然会想到一些改进思路：</p> <p><strong>写入环节</strong>：不依赖 Agent 自主判断，改为每轮对话结束时自动触发结构化提取。用 LLM 做结构化输出，强制提取用户偏好、事实、计划等。</p> <p><strong>晋升环节</strong>：不等 Cron 调度，在提取的同时做实时整合。对每条新事实，向量检索已有记忆，用 LLM 判定是 INSERT（新事实）、UPDATE（更丰富）、SKIP（已存在）还是 DELETE（矛盾过时）。</p> <p><strong>去重环节</strong>：用向量相似度 + LLM 语义判断替代 Jaccard 字面匹配。</p> <p><strong>召回环节</strong>：混合召回（向量 + BM25），不依赖单一搜索后端。</p> <p><strong>衰减策略</strong>：区分对待。个人事实（偏好、过敏等）应该是 Evergreen 的，不衰减；时效性事件按策略淘汰。</p> <p>这些方向的核心思路是：把关键决策从”LLM 自由发挥”变成”LLM 在结构化约束下做语义判断”，在灵活性和确定性之间找到更好的平衡点。</p>]]></content><author><name></name></author><category term="技术"/><category term="AI"/><summary type="html"><![CDATA[how OpenClaw's memory pipeline works and where it falls short]]></summary></entry><entry><title type="html">从 OpenClaw 源码看 Agent 系统设计的三个维度</title><link href="https://xuyuanzhe.github.io/blog/2026/openclaw-design-philosophy/" rel="alternate" type="text/html" title="从 OpenClaw 源码看 Agent 系统设计的三个维度"/><published>2026-04-13T15:00:00+00:00</published><updated>2026-04-13T15:00:00+00:00</updated><id>https://xuyuanzhe.github.io/blog/2026/openclaw-design-philosophy</id><content type="html" xml:base="https://xuyuanzhe.github.io/blog/2026/openclaw-design-philosophy/"><![CDATA[<p>最近花了一些时间翻阅了 OpenClaw 的源码，记录一下我的思考。</p> <p>我不太关心”养虾”本身有多好玩，更想搞清楚的是：它的 Agent 架构到底做了哪些值得学习的设计？</p> <p>整体看下来，我觉得可以从三个维度去拆解：<strong>Prompt Engineering</strong>、<strong>Context Engineering</strong> 和 <strong>Harness Engineering</strong>。分别对应”如何说”、”让 AI 看什么”、”构建怎样的运行环境”。三者层层递进。</p> <h2 id="prompt-engineering不只是写提示词">Prompt Engineering：不只是写提示词</h2> <p>2023 年大家就在讨论怎么写好提示词了，但到了今天，Prompt Engineering 的内涵已经变了——不再是写一段固定的 System Prompt，而是一套动态的组装机制。</p> <h3 id="模块化的-system-prompt">模块化的 System Prompt</h3> <p>OpenClaw 的 System Prompt 是在运行时拼装出来的。核心函数 <code class="language-plaintext highlighter-rouge">buildAgentSystemPrompt()</code> 接收几十个参数，按固定顺序把各个模块像积木一样拼在一起。</p> <p>大概有 23 个模块，包括：身份标识、工具清单、安全准则、Skills 系统、记忆召回、工作区信息、沙箱配置、运行时信息等等。</p> <p>它还区分了三种模式：</p> <ul> <li><strong>full</strong>：主 Agent 与用户对话时，全量加载</li> <li><strong>minimal</strong>：子 Agent 执行任务，只保留核心模块</li> <li><strong>none</strong>：极简场景，基本只有一行身份标识</li> </ul> <blockquote> <h5 id="tip">TIP</h5> <p class="block-tip">区分模式的原因很简单——上下文窗口是有限的，不同场景下没必要全部加载。这个思路在设计自己的 Agent 系统时也适用。</p> </blockquote> <h3 id="markdown-驱动的配置体系">Markdown 驱动的配置体系</h3> <p>OpenClaw 用一组 <code class="language-plaintext highlighter-rouge">.md</code> 文件来管理 Agent 的”灵魂”和”骨架”，运行时动态注入到 System Prompt。为什么用 Markdown？比纯文本多了格式能力（标题、加粗、斜体），同时又能用 Shell 工具方便地读写。</p> <p>几个核心文件：</p> <ul> <li><code class="language-plaintext highlighter-rouge">AGENT.md</code> — 总纲，定义 Agent 的行为规范</li> <li><code class="language-plaintext highlighter-rouge">SOUL.md</code> — 人格特质、说话风格、价值观（修改时必须通知用户）</li> <li><code class="language-plaintext highlighter-rouge">IDENTITY.md</code> — 外在标识：名字、类型、头像</li> <li><code class="language-plaintext highlighter-rouge">USER.md</code> — 用户档案：称呼、偏好、习惯</li> <li><code class="language-plaintext highlighter-rouge">TOOLS.md</code> — 可用工具清单</li> <li><code class="language-plaintext highlighter-rouge">BOOTSTRAP.md</code> — 首次启动的引导脚本，用完即删</li> <li><code class="language-plaintext highlighter-rouge">MEMORY.md</code> — 长期记忆</li> </ul> <h3 id="极简主义的措辞">极简主义的措辞</h3> <p>OpenClaw 的 Prompt 措辞非常精炼。比如要求群聊里不要每条都回复，它用了一句 <code class="language-plaintext highlighter-rouge">Quality &gt; quantity</code>；要求遇到不确定的事情就问用户，用了一句 <code class="language-plaintext highlighter-rouge">Ask anything you're uncertain about</code>。</p> <p>这种风格值得学习。我们写提示词的时候容易越写越啰嗦，试图覆盖各种边界情况，结果 token 消耗巨大，重点反而模糊了。每一个 token 都是有成本的，在需要注入大量配置文件的情况下尤其如此。</p> <h2 id="context-engineering在有限窗口里做更多事">Context Engineering：在有限窗口里做更多事</h2> <p>Prompt Engineering 解决的是”做什么”，Context Engineering 解决的是”如何在有限的上下文窗口里做得更好”。</p> <p>这部分可以从三个角度看：Skills 机制、上下文压缩/修剪、Memory 系统。</p> <h3 id="skills-的渐进式披露">Skills 的渐进式披露</h3> <p>OpenClaw 默认不具备所有能力。它通过一个类似 App Store 的市场（ClawHub）来扩展技能。关键在于：系统不会一次性把所有 Skill 的详细内容塞进上下文，而是先只加载名称和描述，判断需要用哪个，再去读对应的 <code class="language-plaintext highlighter-rouge">SKILL.md</code>。</p> <p>这就是 Anthropic 提出的<strong>渐进式披露（Progressive Disclosure）</strong>——按需加载，避免上下文爆炸。</p> <blockquote> <h5 id="warning">WARNING</h5> <p class="block-warning">Skills 的开放也带来了安全风险。Skill 包里包含可执行脚本，恶意开发者可能植入后门。OpenClaw 近期在这方面加强了鉴权和管控。</p> </blockquote> <h3 id="上下文压缩compaction">上下文压缩（Compaction）</h3> <p>对话越来越长，Context Window 总会不够用。OpenClaw 的策略是：保留最近几轮对话的原文，把更早的历史压缩成摘要。</p> <p>源码在 <code class="language-plaintext highlighter-rouge">src/agents/compaction.ts</code>，核心逻辑：</p> <ol> <li>把旧消息切分成多个 chunk（自适应分块，每块约占上下文的 40%）</li> <li>每个 chunk 独立生成 Summary</li> <li>合并所有 Summary 作为压缩后的历史</li> </ol> <p>压缩时会特别保留：活跃任务、重要决策、TODO、UUID 等标识符（不能让模型自己瞎改）。</p> <p>还有一些工程细节：5 分钟超时保护、压缩期间加写锁、可以配置用便宜的模型来做压缩。</p> <h3 id="修剪pruning">修剪（Pruning）</h3> <p>工具调用的返回结果是占上下文的大户。一个大文件的读取结果可能瞬间消耗数万 token。</p> <p>OpenClaw 的修剪策略是<strong>头尾保留，中间省略</strong>——因为报错信息通常在开头和结尾，JSON 的结构定义也在头部。裁剪比例控制在 50% 以内。</p> <p>两者的区别：</p> <table> <thead> <tr> <th>特性</th> <th>压缩（Compaction）</th> <th>修剪（Pruning）</th> </tr> </thead> <tbody> <tr> <td>做法</td> <td>调用 LLM 生成摘要替换旧消息</td> <td>规则裁剪，直接删减</td> </tr> <tr> <td>信息</td> <td>摘要保留关键信息</td> <td>信息直接丢失</td> </tr> <tr> <td>成本</td> <td>需要额外的 LLM 调用</td> <td>低成本</td> </tr> <tr> <td>场景</td> <td>对话历史太长</td> <td>工具返回结果太大</td> </tr> </tbody> </table> <h3 id="memory-双层管理">Memory 双层管理</h3> <p>大模型每次会话都是”失忆”的。想要跨会话记住东西，就得靠外部存储。</p> <p>OpenClaw 的做法是双层记忆：</p> <ul> <li><strong><code class="language-plaintext highlighter-rouge">MEMORY.md</code>（长期记忆）</strong>：存储高价值信息，每次对话自动注入到 System Prompt（截断到 200 行）</li> <li><strong><code class="language-plaintext highlighter-rouge">memory/日期.md</code>（每日笔记）</strong>：细节化的日常记录，通过搜索访问</li> </ul> <p>写入方面，除了用户主动说”记住这个”之外，会话结束时还会自动触发 Memory Flush，把关键信息归档。</p> <p>读取方面，用 BM25 + 向量匹配双路召回。每日笔记还有时间衰减机制，半衰期 30 天——30 天前的记忆权重减半，90 天前只剩 1/8，模拟人类的”自然遗忘”。</p> <table> <thead> <tr> <th>特性</th> <th>MEMORY.md</th> <th>memory/日期.md</th> </tr> </thead> <tbody> <tr> <td>数量</td> <td>只有一个</td> <td>每天一个</td> </tr> <tr> <td>写入</td> <td>覆盖或编辑</td> <td>追加</td> </tr> <tr> <td>注入方式</td> <td>每次对话自动注入</td> <td>只通过搜索访问</td> </tr> <tr> <td>衰减</td> <td>不衰减</td> <td>随时间衰减</td> </tr> </tbody> </table> <h2 id="harness-engineering给野马套上缰绳">Harness Engineering：给野马套上缰绳</h2> <p>这是一个比较新的概念。Anthropic 和 OpenAI 分别在 2025 年底和 2026 年初提出了相关的论述。</p> <p>简单说：Prompt Engineering 告诉模型”做什么”，Context Engineering 让模型”做得更好”，Harness Engineering 确保模型”可控地做”。</p> <h3 id="为什么需要-harness">为什么需要 Harness</h3> <p>没有 Harness 的 Agent 容易出问题：</p> <ul> <li>写完代码就认为完成了，不跑测试</li> <li>遇到错误陷入死循环</li> <li>执行高风险操作（删文件、调外部 API）时没有确认机制</li> </ul> <p>Harness 就是在模型之外建立一套约束机制，让这些问题有工程化的解决方案。</p> <h3 id="harness-vs-workflow">Harness vs Workflow</h3> <p>这两个概念容易混淆。我的理解是，核心区别在于<strong>主导权</strong>：</p> <ul> <li><strong>Workflow</strong>：人预定义固定路径（A → B → C），模型只是其中一个节点。主导权在人。</li> <li><strong>Harness</strong>：模型仍然有自主规划和迭代的权利，但在关键节点有约束和校验。主导权在模型，但有”缰绳”。</li> </ul> <blockquote> <h5 id="note">Note</h5> <p class="block-tip">在基座模型越来越强的趋势下，Harness 比 Workflow 更能发挥模型本身的能力，同时又不至于完全失控。</p> </blockquote> <h3 id="openclaw-的-harness-实践">OpenClaw 的 Harness 实践</h3> <p><strong>Hook 系统</strong>：在 Agent 运行的关键节点插入自定义逻辑。</p> <table> <thead> <tr> <th>钩子</th> <th>时机</th> <th>用途</th> </tr> </thead> <tbody> <tr> <td>before_tool_call</td> <td>执行工具前</td> <td>参数校验、拦截</td> </tr> <tr> <td>after_tool_call</td> <td>工具执行后</td> <td>结果处理</td> </tr> <tr> <td>before_compaction</td> <td>压缩前</td> <td>标注或观察</td> </tr> <tr> <td>message_received</td> <td>收到消息时</td> <td>预处理</td> </tr> </tbody> </table> <p>比如可以在 <code class="language-plaintext highlighter-rouge">before_tool_call</code> 里用正则校验参数格式，把错误拦截在执行之前。一次配置，所有工具调用都能生效。</p> <p><strong>三层安全沙箱</strong>：文件系统沙箱（限制访问范围）、命令执行沙箱（白名单 + 人工确认）、网络访问沙箱（白名单域名）。</p> <p><strong>Human-in-the-Loop</strong>：遇到不确定或高风险操作时暂停执行，等待人工确认。这是 Harness 赋予人类的最终控制权。</p> <h2 id="一些思考">一些思考</h2> <p>看完 OpenClaw 的源码，我觉得真正有价值的不是它的产品形态，而是这些设计思路：</p> <ol> <li>System Prompt 不要写成一坨，要模块化、按需加载</li> <li>上下文管理是 Agent 系统的核心工程挑战，压缩和修剪缺一不可</li> <li>记忆系统要分层，长期记忆和短期记忆的管理策略完全不同</li> <li>不能指望模型”自觉”，关键节点的约束必须是工程化的</li> </ol> <p>这些东西不绑定 OpenClaw，在设计任何 Agent 系统的时候都适用。</p>]]></content><author><name></name></author><category term="技术"/><category term="AI"/><summary type="html"><![CDATA[what can we learn from OpenClaw's architecture design]]></summary></entry><entry><title type="html">Agent Memory 架构思考：从本质命题到系统设计</title><link href="https://xuyuanzhe.github.io/blog/2026/agent-memory-architecture/" rel="alternate" type="text/html" title="Agent Memory 架构思考：从本质命题到系统设计"/><published>2026-04-07T15:00:00+00:00</published><updated>2026-04-07T15:00:00+00:00</updated><id>https://xuyuanzhe.github.io/blog/2026/agent-memory-architecture</id><content type="html" xml:base="https://xuyuanzhe.github.io/blog/2026/agent-memory-architecture/"><![CDATA[<p>最近读到一篇关于 Agent Memory 架构的长文，信息密度非常高，引用了大量 2026 年初的新论文。读完之后整理一下我觉得最有价值的部分。</p> <p>这篇文章讨论的 Memory 不是指对话历史的简单存储，而是：Agent 在长期交互中积累的、可被检索与利用的记录/知识/经验库。它直接影响个性化、持续学习和长程任务表现。</p> <h2 id="memory-的本质三个核心命题">Memory 的本质：三个核心命题</h2> <p>文章提出了三个命题，我觉得推导过程比结论本身更有价值。</p> <p><strong>命题 A：Memory 不是”存储”，而是可被决策利用的外部状态</strong></p> <p>把 Agent 看成一个函数，”存了很多历史”本身不构成能力。能力来自：历史能否以某种形式影响当前的决策分布。</p> <p>Memory 的价值不在于”存了多少”，而在于从历史到当前决策的通道是否有效。</p> <p><strong>命题 B：Memory 的最小闭包是 (Ledger, Views, Policy) 三件套</strong></p> <p>只要系统需要满足可溯源、可回滚、可观测，Memory 的最小形态就不可能是”一个向量库 + 若干 prompt”。它必须是：</p> <ol> <li><strong>Raw Ledger（权威记录）</strong>：append-only 的事件日志，记录每次写入/更新/删除</li> <li><strong>Derived Views（派生视图）</strong>：面向检索的派生状态（向量索引、KG、timeline、skill index 等），可以 lossy，但必须可回指 Ledger</li> <li><strong>Policy（控制层）</strong>：决定何时读、何时写、如何更新、如何遗忘，决策必须显式化为可记录的 Action 序列</li> </ol> <blockquote> <h5 id="note">Note</h5> <p class="block-tip">类比一下：Raw Ledger 像”账本/黑匣子”；Views 像”缓存 + 索引 + 物化视图”；Policy 像”调度器/控制回路”。三者缺一，系统要么不可治理，要么不可用，要么不可迭代。</p> </blockquote> <p><strong>命题 C：Memory 的基本单位是 event 序列，但直接用 event 流不等于可用系统</strong></p> <p>event 是”真相来源”，但太底层。真正把历史变成能力的是 Views（对 event 的重组织/压缩/索引）和 Policy（决定什么时候触发哪些 view）。</p> <p>event 是 Ledger 的数据形态；Views / Policy 是能力形态。</p> <h2 id="system-1--system-2-的分工">System 1 + System 2 的分工</h2> <p>一个关键判断：<strong>记忆能力和 LLM 本身的 Agent 能力是「相对」正交的</strong>。</p> <p>这意味着我们可以把系统拆成两块低耦合的模块：</p> <ul> <li><strong>System 1</strong>：通用 LLM/Agent（推理、规划、工具调用）</li> <li><strong>System 2</strong>：记忆系统（写入、索引、检索、过滤、摘要、时序化）</li> </ul> <p>为什么要外置化？如果记忆能力只能通过 RL post-training 固化到 LLM 权重里，很难保证特化训练后仍保持通用泛化能力。外置的 System 2 可以做到可插拔、可迁移、更易归因。</p> <p>从经验现象看：同一个 base model 接入不同的记忆策略就能显著改变长程任务表现；同一套记忆 infra 也能服务不同的 base model。这种”跨模型可复用”说明能力更多绑定在接口与外部状态上。</p> <p>System 2 的架构大致是这样的：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>User/Env IO → System 1 (LLM + tools + planner) → Output
                        ↑↓
              System 2: Agentic Memory (Slow Loop)
              PreThink → Retrieve (loop) → Evidence Accumulate → Early Stop
                              ↕
                        Memory Infra
                        - Raw Ledger
                        - Derived Views (Vector/BM25/KG/Timeline)
                        ← control feedback loop →
</code></pre></div></div> <h2 id="非参数化-memory-的上限">非参数化 Memory 的上限</h2> <p>参数化记忆把经验写进模型权重，非参数化把经验写在外部状态里。两者的差别不在”有没有存储”，而在<strong>适应算子写在哪里</strong>。</p> <p>非参数化方案想逼近参数化效果，核心不是把更多东西塞进库，而是让修正项 Δ 逼近”如果真的 fine-tune 了会发生什么”。</p> <p>上限由三类瓶颈共同决定：</p> <p><strong>(1) 接口带宽</strong></p> <p>不管是 passage、graph 还是 skill，最终都要通过某种 Context Bridge 让 System 1 用上它。token 预算、注意力容量都是硬约束。外部记忆再大也没用，真正起作用的是每一步能注入多少”有效信息”。</p> <p><strong>(2) 检索与聚合误差</strong></p> <p>Views 是近似结构，近似就会带来错检、漏检、时序冲突、语义漂移。上限不仅取决于”存了多少”，更取决于误差是否被治理住。</p> <p><strong>(3) Policy 的可学习性与可控性</strong></p> <p>这是最容易被低估的瓶颈：</p> <ul> <li>写多了污染，写少了学不到</li> <li>召回多了噪声，召回少了信息不够</li> <li>UPDATE/DELETE 做错一次，长期滚雪球</li> </ul> <blockquote> <h5 id="warning">WARNING</h5> <p class="block-warning">基于预定义规则（rubrics）的 policy 不可持续。必须把控制回路显式化为可训练对象，让它既能学习（RL 训练范式），又能治理（候选集合约束 + provenance 闭包 + sandbox A/B）。</p> </blockquote> <h2 id="记忆单元的结构时序和压缩">记忆单元的结构、时序和压缩</h2> <h3 id="记忆固化">记忆固化</h3> <p>SimpleMem 不存储原始文本，而是存储压缩后的”记忆单元”。通过亲和力评分将高相似的记忆单元递归合并为更高层级的抽象表征——很像参数化模型训练中的层级抽象。</p> <h3 id="时序记忆">时序记忆</h3> <p>这部分我觉得是全文最有洞察力的地方。</p> <p>LLM 对时间天然不敏感。纯语义检索会把”过去的高相似事实”错当成”现在仍为真”。解决方案不是更强的 prompt，而是把时序提升为架构的结构维度。</p> <p>核心概念是<strong>双时态（bi-temporal）</strong>：</p> <ul> <li><strong>valid_time</strong>：事实在世界中何时为真</li> <li><strong>transaction_time</strong>：系统何时写入/更正</li> </ul> <p>两者不等价——你可以在今天纠正上周发生的事实。</p> <p>遗忘也不应该只是一条全局衰减曲线，而应区分三层抑制机制：</p> <ol> <li><strong>validity gating</strong>：不在有效窗口内的事实，直接不进候选集（硬门控）</li> <li><strong>tombstone</strong>：显式撤回某条记忆的可见性（可审计抑制）</li> <li><strong>decay</strong>：在有效集合里做偏好排序（软信号）</li> </ol> <p>三者的关系是：先 validity，再 tombstone，再 decay。否则会出现”decay 把无效事实拉回来”的语义错误。</p> <p>还有一个重要推演：<strong>记忆固化不能跨越变更点</strong>。如果只按语义相似合并，会把不同时期的不同真值”平均化”。固化的第一原则不是更抽象，而是不跨越变更点。</p> <h2 id="程序性记忆从知识到技能">程序性记忆：从”知识”到”技能”</h2> <p>传统 RAG 擅长检索”信息片段”，但很难直接复用”推理过程/操作流程”。</p> <p>ProcMEM 的思路是把成功的交互轨迹抽象为可执行的 skill，用 Skill-MDP 形式化。一个 skill 是三元结构（触发条件、执行序列、终止条件）。</p> <p>关键设计：</p> <ul> <li><strong>生成</strong>：用语义梯度（Semantic Gradient）对技能做事后归因，生成候选技能</li> <li><strong>验证</strong>：用 PPO 风格的信任域门控做反事实验证，过滤掉激进且不可靠的候选</li> <li><strong>维护</strong>：基于在线评分做进化压力，得分长期为非正的技能被淘汰</li> </ul> <p>经验记忆的三个特点：可执行性、可复用性、非参数化优化（不更新 LLM 权重仍能持续改进）。</p> <h2 id="memory-整合层潜层融合">Memory 整合层：潜层融合</h2> <p>一个工程上绕不过去的问题：外部记忆通常是文本/结构化数据，LLM 的核心计算对象是 token 与注意力。常见实现要经历 外部内容 → 文本化 → 拼 prompt → 模型再编码 的链路，存在编解码开销和信息损失。</p> <p>2026 年的一些工作开始探索 Machine-Native 的记忆形态：</p> <ul> <li><strong>LycheeMemory</strong>：训练 compressor 把输入压缩成潜层 token，直接注入注意力计算，绕开文本中间层</li> <li><strong>MemAdapter</strong>：异构记忆的对齐器，把图谱等结构化信息零样本映射到 LLM 的语义空间</li> </ul> <p>但这里有个盲点：潜层 token 越 machine-native，可读性越弱，越依赖可观测性与溯源机制兜底。</p> <h2 id="架构总结">架构总结</h2> <p>文章最后给出了一个类 Linux 风格的五件套抽象：</p> <ul> <li><strong>内核（Kernel）</strong>：System 2 的慢回路与调度器，决定何时检索/写入/更新/遗忘</li> <li><strong>文件系统（Storage）</strong>：Raw Ledger + 派生视图，承载时序一致性与分层固化</li> <li><strong>可执行文件（Skill）</strong>：把经验固化为可执行、可复用的程序性单元</li> <li><strong>总线接口（Context Bridge）</strong>：记忆与推理之间的接口层，低开销低失真地注入</li> <li><strong>学习引擎（Online Adaptation）</strong>：持续把交互反馈转化为改进信号</li> </ul> <h2 id="我的一些想法">我的一些想法</h2> <p>读完这篇文章，几个对我触动比较大的点：</p> <ol> <li><strong>Memory 不是一个组件，而是一个闭环系统</strong>。之前在分析 OpenClaw 的记忆系统时，就发现它的问题恰好落在这三件套的各个环节上——写入靠 LLM 自由发挥（Policy 缺失）、Jaccard 去重没有语义理解（Views 误差）、晋升靠统计信号而非语义重要性（Policy 不可学习）。</li> <li><strong>时序是架构的结构维度，不是 metadata</strong>。OpenClaw 用简单的时间衰减半衰期来处理”遗忘”，但这篇文章说得对：decay 只是最后一层软信号，前面还需要 validity gating 和 tombstone 这两层硬约束。否则就会出现”decay 把无效事实拉回来”的问题。</li> <li><strong>程序性记忆是一个被低估的方向</strong>。我们大部分时候在讨论的都是 Declarative Memory（事实/证据），但在真实任务中，一段可复用的解决路径（怎么做）往往比一条事实（是什么）价值更高。</li> <li><strong>Policy 是最大的瓶颈</strong>。存储后端不是问题，检索算法也有成熟方案，真正难的是”什么时候写、写什么、信谁”这些控制决策。这也是为什么基于规则的 policy 不可持续——必须让它变成一个可训练的对象。</li> </ol> <hr/> <p><strong>相关论文：</strong></p> <ul> <li>JitRL：推理阶段利用外部经验库调制 logits</li> <li>UMEM：通过语义邻域构建实现记忆的迁移能力</li> <li>AgeMem：将记忆操作工具化并用 RL 训练使用策略</li> <li>InfMem：PreThink-Retrieve-Write 协议，自适应早停</li> <li>SimpleMem：递归固化，模拟生物记忆 Consolidation</li> <li>Zep/Graphiti：时序知识图谱（TKG），bi-temporal 设计</li> <li>MAGMA：四正交图架构（语义/时间/因果/实体）</li> <li>ProcMEM：Skill-MDP，将交互轨迹固化为可执行技能</li> <li>LycheeMemory：潜层压缩与 KV-Cache 注入</li> <li>MemAdapter：异构记忆的零样本对齐</li> <li>MemWeaver：三层混合存储（Graph/Experience/Passage）</li> </ul>]]></content><author><name></name></author><category term="技术"/><category term="AI"/><category term="Agent"/><summary type="html"><![CDATA[rethinking memory architecture for AI agents]]></summary></entry><entry><title type="html">关于学习</title><link href="https://xuyuanzhe.github.io/blog/2026/college/" rel="alternate" type="text/html" title="关于学习"/><published>2026-03-31T15:00:00+00:00</published><updated>2026-03-31T15:00:00+00:00</updated><id>https://xuyuanzhe.github.io/blog/2026/college</id><content type="html" xml:base="https://xuyuanzhe.github.io/blog/2026/college/"><![CDATA[<p>张雪峰很有感染力的一段话:</p> <p>对于学习，不要给孩子讲大道理，</p> <p>你跟他说考个好高中好大学，</p> <p>以后找个好工作，他听不明白的，</p> <p>他看不懂父母为什么苦口婆心</p> <p>的去劝他好好学习，</p> <p>他觉得打工比学习轻松很多。</p> <p>要用孩子的话说给孩子听，</p> <p>给孩子选择的机会。</p> <p>父母是孩子的第一任老师，</p> <p>走进孩子的心，正确引导，静待花开。</p> <p>告诉孩子，考大学是选择人生的第一步。你知道吗，同济大学拥有三十三个食堂，</p> <p>美食应有尽有，还曾上过综艺节目。</p> <p>哈工大在校园里竟然可以发射火箭。</p> <p>华中科技大学有多大?五站地铁都装不下。</p> <p>你喜欢美丽的风景、浪漫的生活吗?</p> <p>那一定不要错过下面这几个大学!</p> <p>武汉大学整个校园都堪比五a景区，</p> <p>走在校园内就能欣赏到浪漫的樱花。</p> <p>躺在厦门大学的宿舍，</p> <p>你能听到海浪声闲时走几分钟，</p> <p>就能到达美丽的白城沙滩。</p> <p>孩子，你现在躺沙发上玩手机很舒服，</p> <p>可是人生要自己去经历，去感受啊!</p> <p>你只知道刷视频，玩游戏，</p> <p>看的是别人的快乐，荒废的是自己的人生。</p> <p>张雪峰曾说过:</p> <p>如果你不好好学习，一旦掉入社会底层，</p> <p>和一群没有素质的人混在一起。</p> <p>你就记住，你永远会被欺负，</p> <p>永无翻身之时。</p> <p>你现在最大的痛苦，只是熬夜背书，</p> <p>学不好最多被老师和父母批评。</p> <p>但是等到将来，你最大的痛苦，</p> <p>是看着同学有房、有车，有完美的伴侣，</p> <p>而你却一无所有，望尘莫及。</p> <p>所以，父母告诉孩子要好好学习，</p> <p>将来考一个好大学，孩子没什么概念，</p> <p>是因为孩子对自己的人生没有选择权。</p> <p>你把残酷的现实摆在他面前</p> <p>告诉孩子，现在读书确实很辛苦，</p> <p>你也没有动力，可你有没有想过以后，</p> <p>如果以后遇到喜欢东西，喜欢的人。</p> <p>你拿什么去追求?</p> <p>孩子其实并非不听父母的话，</p> <p>而是他听不懂，他对未来是无知的。</p> <p>现在的舒适和未来的迷茫，</p> <p>你猜孩子怎么选?</p> <p>所以，才需要父母给孩子种下大学梦，</p> <p>让他对好大学心生向往，并为之努力，</p> <p>这才是父母送给孩子最好的礼物。</p>]]></content><author><name></name></author><category term="阅读"/><category term="家庭"/><category term="教育"/><summary type="html"><![CDATA[张雪峰很有感染力的一段话]]></summary></entry><entry><title type="html">Pyarmor 包</title><link href="https://xuyuanzhe.github.io/blog/2026/code-confusion/" rel="alternate" type="text/html" title="Pyarmor 包"/><published>2026-01-14T15:00:00+00:00</published><updated>2026-01-14T15:00:00+00:00</updated><id>https://xuyuanzhe.github.io/blog/2026/code-confusion</id><content type="html" xml:base="https://xuyuanzhe.github.io/blog/2026/code-confusion/"><![CDATA[<h1 id="尝试使用-pyarmor-包混淆-python-服务">尝试使用 pyarmor 包混淆 python 服务</h1> <h2 id="step1-安装">step1 安装</h2> <p>在虚拟环境中使用 pip 下载</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>uv pip <span class="nb">install </span>pyarmor
</code></pre></div></div> <p>查看版本</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>uv pip list | <span class="nb">grep </span>pyarmor

<span class="c"># 输出</span>
Using Python 3.12.12 environment at: .env-multi-agent
pyarmor                                           9.2.3
pyarmor-cli-core                                  8.1.0
</code></pre></div></div> <h2 id="step2-准备工作">step2 准备工作</h2> <p>确保服务可以正常启动</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 进入项目</span>
<span class="nb">cd</span> /path/to/your/project

<span class="c"># 启动服务</span>
uvicorn main:app <span class="nt">--reload</span>

<span class="c"># 服务正常启动会输出</span>
INFO:     Will watch <span class="k">for </span>changes <span class="k">in </span>these directories: <span class="o">[</span><span class="s1">'/path/to/your/project'</span><span class="o">]</span>
INFO:     Uvicorn running on http://127.0.0.1:8000 <span class="o">(</span>Press CTRL+C to quit<span class="o">)</span>
INFO:     Started reloader process <span class="o">[</span>54433] using WatchFiles
__start__
__start__
__start__
INFO:     Started server process <span class="o">[</span>54435]
INFO:     Waiting <span class="k">for </span>application startup.
INFO:     Application startup complete.
</code></pre></div></div> <h2 id="step3-混淆">step3 混淆</h2> <p>第一次尝试使用 pyarmor 混淆</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pyarmor gen <span class="nt">--output</span><span class="o">=</span>dist <span class="nt">--include</span><span class="o">=</span><span class="s2">"**/*.py"</span> <span class="nb">.</span>
</code></pre></div></div> <p>遇到报错信息如下</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>INFO     Python 3.12.12
INFO     Pyarmor 9.2.3 <span class="o">(</span>trial<span class="o">)</span>, 000000, non-profits
INFO     Platform darwin.x86_64
INFO     search inputs ...
INFO     find package at <span class="nb">.</span>
INFO     find 1 top resources
...
INFO     write dist/./.env-multi-agent/lib/python3.12/site-packages/nest_asyncio.py
INFO     obfuscating file typing_extensions.py
ERROR    out of license

Pyarmor 9.2.3 <span class="o">(</span>trial<span class="o">)</span>, 000000, non-profits
</code></pre></div></div> <p>查询后发现，通过 pip 安装的 pyarmor 9.2.3版本为 trial（实验）版，混淆过多文件会提示超出限制。</p> <p>随后查看 trial 版支持的命令用法</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pyarmor gen <span class="nt">-h</span>
</code></pre></div></div> <p>支持参数如下：</p> <table> <thead> <tr> <th>参数</th> <th>简写</th> <th>类型/值</th> <th>中文说明</th> <th>使用建议</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">-O PATH</code></td> <td><code class="language-plaintext highlighter-rouge">--output PATH</code></td> <td>路径字符串</td> <td>指定混淆后文件的输出目录</td> <td>必填，例如 <code class="language-plaintext highlighter-rouge">-O dist</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">-r</code></td> <td><code class="language-plaintext highlighter-rouge">--recursive</code></td> <td>无值（开关）</td> <td>递归处理子目录中的 <code class="language-plaintext highlighter-rouge">.py</code> 文件</td> <td>处理包结构时必加</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">--exclude PATTERN</code></td> <td>—</td> <td>路径模式（可多次使用）</td> <td>排除匹配的文件或目录（支持通配符）</td> <td>用于跳过虚拟环境、缓存等，如 <code class="language-plaintext highlighter-rouge">--exclude .env-multi-agent</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">--obf-module {0,1}</code></td> <td>—</td> <td><code class="language-plaintext highlighter-rouge">0</code> 或 <code class="language-plaintext highlighter-rouge">1</code>（默认 <code class="language-plaintext highlighter-rouge">1</code>）</td> <td>是否混淆整个模块（文件）<br/><code class="language-plaintext highlighter-rouge">1</code>=是，<code class="language-plaintext highlighter-rouge">0</code>=否</td> <td>一般保持默认</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">--obf-code {0,1,2}</code></td> <td>—</td> <td><code class="language-plaintext highlighter-rouge">0</code>/<code class="language-plaintext highlighter-rouge">1</code>/<code class="language-plaintext highlighter-rouge">2</code>（默认 <code class="language-plaintext highlighter-rouge">1</code>）</td> <td>函数级代码混淆强度：<br/><code class="language-plaintext highlighter-rouge">0</code>=不混淆函数体<br/><code class="language-plaintext highlighter-rouge">1</code>=基本混淆<br/><code class="language-plaintext highlighter-rouge">2</code>=增强混淆（更慢但更安全）</td> <td>试用版可能限制 <code class="language-plaintext highlighter-rouge">2</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">--no-wrap</code></td> <td>—</td> <td>无值</td> <td>禁用“包装模式”（wrap mode）</td> <td>高级选项，一般不用</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">--mix-str</code></td> <td>—</td> <td>无值</td> <td>混淆字符串常量（防明文提取）</td> <td>增强保护，推荐开启</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">--assert-call</code></td> <td>—</td> <td>无值</td> <td>运行时检查函数是否被混淆（防篡改）</td> <td>增强安全性</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">--assert-import</code></td> <td>—</td> <td>无值</td> <td>运行时检查模块是否被混淆</td> <td>同上</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">--enable {jit,bcc,rft,themida}</code></td> <td>—</td> <td>特性名</td> <td>启用高级混淆技术（部分需专业版）</td> <td>试用版通常不可用</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">--private</code></td> <td>—</td> <td>无值</td> <td>启用私有模式（脚本级隔离）</td> <td>适用于单脚本分发</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">--restrict</code></td> <td>—</td> <td>无值</td> <td>启用包限制模式（防止被其他代码导入）</td> <td>保护核心模块时可用</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">-i</code></td> <td>—</td> <td>无值</td> <td>将运行时文件嵌入到输出包内（不生成独立 <code class="language-plaintext highlighter-rouge">pyarmor_runtime_xxx</code> 目录）</td> <td>方便分发，减少文件数</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">--prefix PREFIX</code></td> <td>—</td> <td>字符串</td> <td>为运行时包设置导入前缀（用于多版本共存）</td> <td>高级场景使用</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">--platform NAME</code></td> <td>—</td> <td>平台名（如 <code class="language-plaintext highlighter-rouge">linux.x86_64</code>）</td> <td>交叉平台混淆（在 A 平台生成 B 平台代码）</td> <td>跨平台部署时使用</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">--outer</code></td> <td>—</td> <td>无值</td> <td>启用外部运行时密钥（配合授权绑定）</td> <td>需专业版</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">-e DATE</code></td> <td><code class="language-plaintext highlighter-rouge">--expired DATE</code></td> <td>日期（如 <code class="language-plaintext highlighter-rouge">2025-12-31</code>）</td> <td>设置代码过期时间</td> <td>试用版可能不支持</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">--period N</code></td> <td>—</td> <td>整数（天数）</td> <td>定期检查运行时密钥有效性</td> <td>配合授权使用</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">-b DEV</code></td> <td><code class="language-plaintext highlighter-rouge">--bind-device DEV</code></td> <td>设备标识（如 <code class="language-plaintext highlighter-rouge">mac=aa:bb:cc:dd:ee:ff</code>）</td> <td>绑定到特定设备</td> <td>需专业版</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">--bind-data STRING or @FILENAME</code></td> <td>—</td> <td>字符串或 <code class="language-plaintext highlighter-rouge">@文件路径</code></td> <td>在运行时密钥中嵌入自定义数据</td> <td>高级授权用途</td> </tr> </tbody> </table> <p>开始尝试混淆单个文件</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pyarmor gen <span class="nt">--output</span><span class="o">=</span>dist example/test.py
</code></pre></div></div> <p>看到正常输出后说明参数正确，开始尝试白名单混淆整个项目</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pyarmor gen <span class="nt">-O</span> dist <span class="nt">-r</span> main.py app controller core model
</code></pre></div></div> <p>正确的输出了 dist 文件夹且命令行无报错，进入dist 文件夹查看服务是否可以正常启动</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 进入项目</span>
<span class="nb">cd </span>dist

<span class="c"># 启动服务</span>
uvicorn main:app <span class="nt">--host</span> 0.0.0.0 <span class="nt">--port</span> 8000

<span class="c"># 服务正常启动会输出</span>
__start__
__start__
__start__
INFO:     Started server process <span class="o">[</span>53613]
INFO:     Waiting <span class="k">for </span>application startup.
INFO:     Application startup complete.
INFO:     Uvicorn running on http://0.0.0.0:8000 <span class="o">(</span>Press CTRL+C to quit<span class="o">)</span>
</code></pre></div></div> <p>服务启动正常且文件混淆成功，证明 pyarmor 的 trial 版本是满足混淆预期的。</p>]]></content><author><name></name></author><category term="技术"/><category term="Python"/><category term="教学"/><summary type="html"><![CDATA[Python 混淆技术]]></summary></entry><entry><title type="html">AI Agent 记忆系统</title><link href="https://xuyuanzhe.github.io/blog/2025/memory/" rel="alternate" type="text/html" title="AI Agent 记忆系统"/><published>2025-12-10T15:00:00+00:00</published><updated>2025-12-10T15:00:00+00:00</updated><id>https://xuyuanzhe.github.io/blog/2025/memory</id><content type="html" xml:base="https://xuyuanzhe.github.io/blog/2025/memory/"><![CDATA[<h1 id="背景">背景</h1> <p>记忆系统可以使 AI Agent 像人类一样，在单次对话中保持上下文连贯性（短期记忆），同时能够跨会话记住用户偏好、历史交互和领域知识（长期记忆）。 记忆使它们能够记住之前的互动、从反馈中学习，并适应用户的偏好。</p> <h1 id="记忆的定义">记忆的定义</h1> <p>习惯上，可以将会话级别的历史消息称为短期记忆，把可以跨会话共享的信息称为长期记忆。</p> <ul> <li>会话级记忆：用户和智能体 Agent 在一个会话中的多轮交互（user-query &amp; response）</li> <li>跨会话记忆：从用户和智能体 Agent 的多个会话中抽取的通用信息，可以跨会话辅助 Agent 推理</li> </ul> <p>本质上两者并不是通过简单的时间维度进行的划分，从实践层面上以是否跨 Session 会话来进行区分。 长期记忆的信息从短期记忆中抽取提炼而来，根据短期记忆中的信息实时地更新迭代，而其信息又会参与到短期记忆中辅助模型进行个性化推理。</p> <h1 id="agent-框架集成记忆的通用模式">Agent 框架集成记忆的通用模式</h1> <ol> <li>推理前加载- 根据当前 user-query 从长期记忆中加载相关信息</li> <li>上下文注入- 从长期记忆中检索的信息加入当前短期记忆中辅助模型推理</li> <li>记忆更新- 短期记忆在推理完成后加入到长期记忆中</li> <li>信息处理- 长期记忆模块中结合 LLM+向量化模型进行信息提取和检索</li> </ol> <pre><code class="language-mermaid">flowchart LR
    A[用户输入] --&gt; B[短期记忆]
    B --&gt; C[LLM + 工具调用]
    C --&gt; D[短期记忆]
    D --&gt; E[用户回复]
    D --&gt; F[存入长期记忆]
    G[长期记忆] --&gt;|检索| B
    F --&gt; G
</code></pre> <h1 id="短期记忆">短期记忆</h1> <p>短期记忆存储会话中产生的各类消息，包括用户输入、模型回复、工具调用及其结果等。 这些消息直接参与模型推理，实时更新，并受模型的 maxToken 限制。 当消息累积导致上下文窗口超出限制时，需要通过上下文工程策略（压缩、卸载、摘要等）进行处理，这也是上下文工程主要处理的部分。</p> <h3 id="短期记忆的核心特点">短期记忆的核心特点：</h3> <ul> <li>存储会话中的所有交互消息（用户输入、模型回复、工具调用等）</li> <li>直接参与模型推理，作为 LLM 的输入上下文</li> <li>实时更新，每次交互都会新增消息</li> <li>受模型 maxToken 限制，需要上下文工程策略进行优化</li> </ul> <h3 id="短期记忆的上下文工程策略">短期记忆的上下文工程策略</h3> <p>短期记忆直接参与 Agent 和 LLM 的交互，随着对话历史增长，上下文窗口会面临 token 限制和成本压力。 上下文工程策略旨在通过智能化的压缩、卸载和摘要技术，在保持信息完整性的同时，有效控制上下文大小。</p> <p>需要说明的是，各方对上下文工程的概念和理解存在些许差异：</p> <ul> <li>狭义的上下文工程特指对短期记忆（会话历史）中各种压缩、摘要、卸载等处理机制，主要解决上下文窗口限制和 token 成本问题；</li> <li>广义的上下文工程则包括更广泛的上下文优化策略，如非运行态的模型选择、Prompt 优化工程、知识库构建、工具集构建等。</li> </ul> <p>这些都是在模型推理前对上下文进行优化的手段，且这些因素都对模型推理结果有重要影响。</p> <h3 id="针对短期记忆的上下文处理主要有以下几种策略">针对短期记忆的上下文处理，主要有以下几种策略：</h3> <ol> <li>上下文缩减（Context Reduction） ：上下文缩减通过减少上下文中的信息量来降低 token 消耗，主要有两种方法： <ol> <li>保留预览内容：对于大块内容，只保留前 N 个字符或关键片段作为预览，原始完整内容被移除</li> <li>总结摘要：使用 LLM 对整段内容进行总结摘要，保留关键信息，丢弃细节</li> </ol> </li> <li>上下文卸载（Context Unloading） ：主要解决被缩减的内容是否可恢复的问题。当内容被缩减后，原始完整内容被卸载到外部存储（如文件系统、数据库等），消息中只保留最小必要的引用（如文件路径、UUID 等）。当需要完整内容时，可以通过引用重新加载。优势在于上下文更干净，占用更小，信息不丢，随取随用。适用于网页搜索结果、超长工具输出、临时计划等占 token 较多的内容。</li> <li>上下文隔离（Context Isolation）：通过多智能体架构，将上下文拆分到不同的子智能体中（类似单体拆分称多个微服务）。主智能体编写任务指令，发送给子智能体，子智能体的整个上下文仅由该指令组成。子智能体完成任务后返回结果，主智能体不关心子智能体如何执行，只需要结果。优势在于上下文小、开销低、简单直接。适用于任务有清晰简短的指令，只有最终输出才重要，如代码库中搜索特定片段。</li> </ol> <h3 id="策略选择原则">策略选择原则</h3> <ul> <li>时间远近：近期消息通常更重要，需要优先保留；历史消息可以优先进行缩减或卸载；</li> <li>数据类型：不同类型的消息（用户输入、模型回复、工具调用结果等）重要性不同，需要采用不同的处理策略；</li> <li>信息可恢复性：对于需要完整信息的内容，应优先使用卸载策略；对于可以接受信息丢失的内容，可以使用缩减策略</li> </ul> <h1 id="长期记忆">长期记忆</h1> <p>长期记忆与短期记忆形成双向交互：一方面，长期记忆从短期记忆中提取“事实”、“偏好”、“经验”等有效信息进行存储（Record）； 另一方面，长期记忆中的信息会被检索并注入到短期记忆中，辅助模型进行个性化推理（Retrieve）。</p> <p>要实现这两个核心流程，需要以下组件：</p> <ol> <li>LLM 大模型：提取短期记忆中的有效信息（记忆的语义理解、抽取、决策和生成）；</li> <li>Embedder 向量化：将文本转换为语义向量，支持相似性计算；</li> <li>VectorStore 向量数据库：持久化存储记忆向量和元数据，支持高效语义检索；</li> <li>GraphStore 图数据库：存储实体-关系知识图谱，支持复杂关系推理；</li> <li>Reranker（重排序器）：对初步检索结果按语义相关性重新排序；</li> <li>SQLite：记录所有记忆操作的审计日志，支持版本回溯；</li> </ol> <h3 id="record">Record</h3> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>LLM 事实提取 → 信息向量化 → 向量存储 →（复杂关系存储）→ SQLite 操作日志
</code></pre></div></div> <h3 id="retrieve">Retrieve</h3> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>User query 向量化 → 向量数据库语义检索 → 图数据库关系补充 →（Reranker-LLM）→ 结果返回
</code></pre></div></div> <h3 id="与短期记忆的交互">与短期记忆的交互</h3> <ul> <li>Record（写入）：从短期记忆的会话消息中提取有效信息，通过LLM进行语义理解和抽取，存储到长期记忆中</li> <li>Retrieve（检索）：根据当前用户查询，从长期记忆中检索相关信息，注入到短期记忆中作为上下文，辅助模型推理</li> </ul> <h3 id="实践中的实现方式">实践中的实现方式</h3> <p>在 Agent 开发实践中，长期记忆通常是一个独立的第三方组件，因为其内部有相对比较复杂的流程（信息提取、向量化、存储、检索等）。 常见的长期记忆组件包括 Mem0、Zep、Memos、ReMe 等，这些组件提供了完整的 Record 和 Retrieve 能力，Agent 框架通过 API 集成这些组件。</p> <h3 id="信息组织维度">信息组织维度</h3> <p>用户维度（个人记忆）：面向用户维度组织的实时更新的个人知识库</p> <ul> <li>用户画像分析报告</li> <li>个性化推荐系统，千人千面</li> <li>处理具体任务时加载至短期记忆中</li> </ul> <p>业务领域维度：沉淀的经验（包括领域经验和工具使用经验）</p> <ul> <li>可沉淀至领域知识库</li> <li>可通过强化学习微调沉淀至模型</li> </ul> <h3 id="长期记忆与-rag-的区别">长期记忆与 RAG 的区别</h3> <table> <thead> <tr> <th>对比条目</th> <th>RAG</th> <th>长期记忆</th> </tr> </thead> <tbody> <tr> <td><strong>主要目的</strong></td> <td>为大模型提供外部知识，弥补训练数据的局限性（如时效性、专业性）</td> <td>为 AI Agent 记录并利用特定用户的历史交互信息，实现个性化、上下文连续的服务</td> </tr> <tr> <td><strong>服务对象</strong></td> <td>全体用户或任务</td> <td>特定用户或会话主体（高度个性化）</td> </tr> <tr> <td><strong>知识来源</strong></td> <td>结构化/非结构化文档（如 PDF、网页、数据库）</td> <td>用户与 Agent 的对话历史、行为日志等</td> </tr> </tbody> </table> <p>技术层面的相似点：</p> <ol> <li>向量化存储：都将文本内容通过 Embedding 模型转为向量，存入向量数据库；</li> <li>相似性检索：在用户提问时，将当前 query 向量化，在向量库中检索 top-k 最相关的条目；</li> <li>注入上下文生成：将检索到的内容注入到模型交互上下文中，辅助 LLM 生成最终回答；</li> </ol> <h3 id="长期记忆的关键问题">长期记忆的关键问题</h3> <p>一、准确性</p> <p>记忆的准确性包含两个层面：</p> <ul> <li>有效的记忆管理：需要具备智能的巩固、更新和遗忘机制，这主要依赖于记忆系统中负责信息提取的模型能力和算法设计；</li> <li>记忆相关性的检索准确度：主要依赖于向量化检索&amp;重排的核心能力；</li> </ul> <p>核心挑战：</p> <ul> <li>记忆的建模：需要完善强大的用户画像模型；</li> <li>记忆的管理：基于用户画像建模算法，提取有效信息，设计记忆更新机制；</li> <li>向量化相关性检索能力：提升检索准确率和相关性；</li> </ul> <p>二、安全和隐私</p> <p>记忆系统记住了大量用户隐私信息，如何防止数据中毒等恶意攻击，并保障用户隐私，是必须解决的问题。</p> <p>核心挑战：</p> <ul> <li>数据加密与访问控制</li> <li>防止恶意数据注入</li> <li>透明的数据管理机制</li> <li>用户对自身数据的掌控权</li> </ul> <p>三、多模态记忆</p> <p>支持文本记忆、视觉、语音仍被孤立处理，如何构建统一的“多模态记忆空间”仍是未解难题。</p> <p>核心挑战：</p> <ul> <li>跨模态关联</li> <li>与检索统一的多模态记忆表示</li> <li>毫秒级响应能力</li> </ul> <h3 id="长期记忆开源产品对比">长期记忆开源产品对比</h3> <table> <thead> <tr> <th>产品</th> <th>开源时间</th> <th>功能（记忆类型）</th> <th>LLM的作用</th> <th>向量数据库</th> <th>图数据库</th> </tr> </thead> <tbody> <tr> <td><strong>Mem0</strong></td> <td>2023年7月</td> <td>✅ 用户画像 + 领域记忆</td> <td>记忆创建/更新/检索</td> <td>✅ pgvector, Qdrant等多种</td> <td>✅ Neo4j, Memgraph</td> </tr> <tr> <td><strong>Zep</strong></td> <td>2023年初</td> <td>✅ 用户画像 + 领域记忆</td> <td>记忆提取 + 实体识别</td> <td>✅ 支持</td> <td>✅ 时序知识图谱架构</td> </tr> <tr> <td><strong>MemOS</strong></td> <td>2025年7月</td> <td>✅ 用户画像 + 领域记忆</td> <td>记忆提取 + 推理</td> <td>⚠️ 可选支持，非必需</td> <td>✅ NebulaGraph, Neo4j</td> </tr> <tr> <td><strong>Memobase</strong></td> <td>2025年1月</td> <td>✅ 用户画像记忆<br/>❌ 领域记忆</td> <td>画像理解 + 更新</td> <td>❌ 不依赖向量数据库</td> <td>❌ 不依赖图数据库</td> </tr> <tr> <td><strong>AgentScope ReMe</strong></td> <td>2025年6月</td> <td>✅ 用户画像 + 领域记忆</td> <td>记忆管理 + 优化</td> <td>✅ Chroma, Qdrant等多种</td> <td>❌ 未明确提及</td> </tr> </tbody> </table> <h1 id="mem0mem-zero">mem0(“mem-zero”)</h1> <h2 id="自托管">自托管</h2> <p>1、安装 Mem0</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pip <span class="nb">install </span>mem0ai openai
</code></pre></div></div> <p>2、一个小的 demo</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">openai</span> <span class="kn">import</span> <span class="n">OpenAI</span>
<span class="kn">from</span> <span class="n">mem0</span> <span class="kn">import</span> <span class="n">Memory</span>

<span class="n">openai_client</span> <span class="o">=</span> <span class="nc">OpenAI</span><span class="p">()</span>
<span class="n">memory</span> <span class="o">=</span> <span class="nc">Memory</span><span class="p">()</span>

<span class="k">def</span> <span class="nf">chat_with_memories</span><span class="p">(</span><span class="n">message</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">user_id</span><span class="p">:</span> <span class="nb">str</span> <span class="o">=</span> <span class="sh">"</span><span class="s">default_user</span><span class="sh">"</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>

    <span class="c1"># 检索相关记忆
</span>    <span class="n">relevant_memories</span> <span class="o">=</span> <span class="n">memory</span><span class="p">.</span><span class="nf">search</span><span class="p">(</span><span class="n">query</span><span class="o">=</span><span class="n">message</span><span class="p">,</span> <span class="n">user_id</span><span class="o">=</span><span class="n">user_id</span><span class="p">,</span> <span class="n">limit</span><span class="o">=</span><span class="mi">3</span><span class="p">)</span>
    <span class="n">memories_str</span> <span class="o">=</span> <span class="sh">"</span><span class="se">\n</span><span class="sh">"</span><span class="p">.</span><span class="nf">join</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">- </span><span class="si">{</span><span class="n">entry</span><span class="p">[</span><span class="sh">'</span><span class="s">memory</span><span class="sh">'</span><span class="p">]</span><span class="si">}</span><span class="sh">"</span> <span class="k">for</span> <span class="n">entry</span> <span class="ow">in</span> <span class="n">relevant_memories</span><span class="p">[</span><span class="sh">"</span><span class="s">results</span><span class="sh">"</span><span class="p">])</span>

    <span class="c1"># 生成助手回复
</span>    <span class="n">system_prompt</span> <span class="o">=</span> <span class="sa">f</span><span class="sh">"</span><span class="s">你是我的私人助理，请根据我的记忆回答我的问题。</span><span class="se">\n</span><span class="s">我的记忆：</span><span class="se">\n</span><span class="si">{</span><span class="n">memories_str</span><span class="si">}</span><span class="sh">"</span>
    <span class="n">messages</span> <span class="o">=</span> <span class="p">[{</span><span class="sh">"</span><span class="s">role</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">system</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">content</span><span class="sh">"</span><span class="p">:</span> <span class="n">system_prompt</span><span class="p">},</span> <span class="p">{</span><span class="sh">"</span><span class="s">role</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">user</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">content</span><span class="sh">"</span><span class="p">:</span> <span class="n">message</span><span class="p">}]</span>
    <span class="n">response</span> <span class="o">=</span> <span class="n">openai_client</span><span class="p">.</span><span class="n">chat</span><span class="p">.</span><span class="n">completions</span><span class="p">.</span><span class="nf">create</span><span class="p">(</span><span class="n">model</span><span class="o">=</span><span class="sh">"</span><span class="s">gpt-4o-mini</span><span class="sh">"</span><span class="p">,</span> <span class="n">messages</span><span class="o">=</span><span class="n">messages</span><span class="p">)</span>
    <span class="n">assistant_response</span> <span class="o">=</span> <span class="n">response</span><span class="p">.</span><span class="n">choices</span><span class="p">[</span><span class="mi">0</span><span class="p">].</span><span class="n">message</span><span class="p">.</span><span class="n">content</span>

    <span class="c1"># 从对话中创建新记忆
</span>    <span class="n">messages</span><span class="p">.</span><span class="nf">append</span><span class="p">({</span><span class="sh">"</span><span class="s">role</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">assistant</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">content</span><span class="sh">"</span><span class="p">:</span> <span class="n">assistant_response</span><span class="p">})</span>
    <span class="n">memory</span><span class="p">.</span><span class="nf">add</span><span class="p">(</span><span class="n">messages</span><span class="p">,</span> <span class="n">user_id</span><span class="o">=</span><span class="n">user_id</span><span class="p">)</span>

    <span class="k">return</span> <span class="n">assistant_response</span>

<span class="k">def</span> <span class="nf">main</span><span class="p">():</span>
    <span class="k">while</span> <span class="bp">True</span><span class="p">:</span>
        <span class="n">user_input</span> <span class="o">=</span> <span class="nf">input</span><span class="p">(</span><span class="sh">"</span><span class="s">用户：</span><span class="sh">"</span><span class="p">).</span><span class="nf">strip</span><span class="p">()</span>
        <span class="k">if</span> <span class="n">user_input</span><span class="p">.</span><span class="nf">lower</span><span class="p">()</span> <span class="o">==</span> <span class="sh">'</span><span class="s">exit</span><span class="sh">'</span><span class="p">:</span>
            <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">再见！</span><span class="sh">"</span><span class="p">)</span>
            <span class="k">break</span>
        <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">系统：</span><span class="si">{</span><span class="nf">chat_with_memories</span><span class="p">(</span><span class="n">user_input</span><span class="p">,</span> <span class="sh">"</span><span class="s">zhangsan</span><span class="sh">"</span><span class="p">)</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>

<span class="k">if</span> <span class="n">__name__</span> <span class="o">==</span> <span class="sh">"</span><span class="s">__main__</span><span class="sh">"</span><span class="p">:</span>
    <span class="nf">main</span><span class="p">()</span>

</code></pre></div></div> <p>特点：<br/> 用户不再关注和拼接历史会话，每次用户请求进来后，先检索记忆，然后将记忆带到系统 Prompt 中回答用户问题，最后再将这次对话保存到记忆，以此循环往复。</p> <p>注意：<br/> 上面这个记忆保存在内存里，只是临时的，重启程序后记忆就没有了。</p> <h2 id="平台托管">平台托管</h2> <p>进入 Mem0 平台，注册后，根据提示步骤获取 API KEY。</p> <p>修改 demo 代码：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">mem0</span> <span class="kn">import</span> <span class="n">MemoryClient</span>
<span class="n">memory</span> <span class="o">=</span> <span class="nc">MemoryClient</span><span class="p">(</span><span class="n">api_key</span><span class="o">=</span><span class="n">os</span><span class="p">.</span><span class="nf">getenv</span><span class="p">(</span><span class="sh">"</span><span class="s">MEM0_API_KEY</span><span class="sh">"</span><span class="p">))</span>
</code></pre></div></div> <p>这样就可以把记忆保存在 Mem0 服务中，就算程序重启，记忆也不会丢失。</p> <p>可以在 Mem0 平台的 “Memories” 页面查看保存的记忆。</p> <h2 id="记忆存储原理">记忆存储原理</h2> <ul> <li>提取阶段（Extraction）</li> <li>更新阶段（Update）</li> </ul> <h3 id="提取阶段">提取阶段</h3> <p>关注信息：</p> <ol> <li>最新的一轮对话，通常由用户消息和助手响应组成；</li> <li>滚动摘要，从向量数据库检索得到，代表整个历史对话的语义内容；</li> <li>最近的 m 条消息，提供了细粒度的时间上下文，可能包含摘要中未整合的相关细节；</li> </ol> <p>然后通过 LLM 从这些信息中提取出一组简明扼要的候选记忆，并在后台异步地刷新对话摘要，刷新过程不阻塞主流程。</p> <h3 id="更新阶段">更新阶段</h3> <p>针对新消息从向量数据库中检索出最相似的前 s 个条目进行比较，然后通过 LLM 的工具调用能力，选择四种操作之一：</p> <ul> <li>ADD - 在没有语义等效记忆存在时创建新记忆；</li> <li>UPDATE - 用补充信息增强现有记忆；</li> <li>DELETE - 删除和新信息所矛盾的记忆；</li> <li>NOOP - 当候选事实不需要对知识库进行修改时；</li> </ul> <p>使得记忆存储保持一致、无冗余，并能立即准备好应对下一个查询。</p> <h2 id="记忆储存数据库">记忆储存数据库</h2> <p>Mem0 的默认存储是 Qdrant 向量数据库，但是使用了本地文件，可以在 /tmp/qdrant 中找到，每次程序启动时都会删掉重建。</p> <p>通过修改 Memory.from_config() 自定义记忆配置，可以指定数据库位置并可以开启持久化存储：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">config</span> <span class="o">=</span> <span class="p">{</span>
    <span class="sh">"</span><span class="s">vector_store</span><span class="sh">"</span><span class="p">:</span> <span class="p">{</span>
        <span class="sh">"</span><span class="s">provider</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">qdrant</span><span class="sh">"</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">config</span><span class="sh">"</span><span class="p">:</span> <span class="p">{</span>
            <span class="sh">"</span><span class="s">path</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">/tmp/qdrant_data</span><span class="sh">"</span><span class="p">,</span>
            <span class="sh">"</span><span class="s">on_disk</span><span class="sh">"</span><span class="p">:</span> <span class="bp">True</span>
        <span class="p">}</span>
    <span class="p">},</span>
<span class="p">}</span>

<span class="n">memory</span> <span class="o">=</span> <span class="n">Memory</span><span class="p">.</span><span class="nf">from_config</span><span class="p">(</span><span class="n">config</span><span class="p">)</span>
</code></pre></div></div> <p>也可以本地部署一个 Qdrant 数据库，并自定义配置：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">config</span> <span class="o">=</span> <span class="p">{</span>
    <span class="sh">"</span><span class="s">vector_store</span><span class="sh">"</span><span class="p">:</span> <span class="p">{</span>
        <span class="sh">"</span><span class="s">provider</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">qdrant</span><span class="sh">"</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">config</span><span class="sh">"</span><span class="p">:</span> <span class="p">{</span>
            <span class="sh">"</span><span class="s">collection_name</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">test</span><span class="sh">"</span><span class="p">,</span>
            <span class="sh">"</span><span class="s">host</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">localhost</span><span class="sh">"</span><span class="p">,</span>
            <span class="sh">"</span><span class="s">port</span><span class="sh">"</span><span class="p">:</span> <span class="mi">6333</span><span class="p">,</span>
        <span class="p">}</span>
    <span class="p">},</span>
<span class="p">}</span>

<span class="n">memory</span> <span class="o">=</span> <span class="n">Memory</span><span class="p">.</span><span class="nf">from_config</span><span class="p">(</span><span class="n">config</span><span class="p">)</span>
</code></pre></div></div> <p>再进一步，我们可以配置自己的向量存储，格式如下：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">config</span> <span class="o">=</span> <span class="p">{</span>
    <span class="sh">"</span><span class="s">vector_store</span><span class="sh">"</span><span class="p">:</span> <span class="p">{</span>
        <span class="sh">"</span><span class="s">provider</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">your_chosen_provider</span><span class="sh">"</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">config</span><span class="sh">"</span><span class="p">:</span> <span class="p">{</span>
            <span class="c1"># Provider-specific settings go here
</span>        <span class="p">}</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <p>备注：有关向量存储的自定义配置可以参考 https://docs.mem0.ai/components/vectordbs/config</p> <h2 id="其他配置">其他配置</h2> <p>除了向量存储配置还可以配置包括：语言模型、嵌入模型、图存储 以及一些通用配置，如下：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">config</span> <span class="o">=</span> <span class="p">{</span>
    <span class="sh">"</span><span class="s">vector_store</span><span class="sh">"</span><span class="p">:</span> <span class="p">{</span>
        <span class="sh">"</span><span class="s">provider</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">qdrant</span><span class="sh">"</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">config</span><span class="sh">"</span><span class="p">:</span> <span class="p">{</span>
            <span class="sh">"</span><span class="s">host</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">localhost</span><span class="sh">"</span><span class="p">,</span>
            <span class="sh">"</span><span class="s">port</span><span class="sh">"</span><span class="p">:</span> <span class="mi">6333</span>
        <span class="p">}</span>
    <span class="p">},</span>
    <span class="sh">"</span><span class="s">llm</span><span class="sh">"</span><span class="p">:</span> <span class="p">{</span>
        <span class="sh">"</span><span class="s">provider</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">openai</span><span class="sh">"</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">config</span><span class="sh">"</span><span class="p">:</span> <span class="p">{</span>
            <span class="sh">"</span><span class="s">api_key</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">your-api-key</span><span class="sh">"</span><span class="p">,</span>
            <span class="sh">"</span><span class="s">model</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">gpt-4</span><span class="sh">"</span>
        <span class="p">}</span>
    <span class="p">},</span>
    <span class="sh">"</span><span class="s">embedder</span><span class="sh">"</span><span class="p">:</span> <span class="p">{</span>
        <span class="sh">"</span><span class="s">provider</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">openai</span><span class="sh">"</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">config</span><span class="sh">"</span><span class="p">:</span> <span class="p">{</span>
            <span class="sh">"</span><span class="s">api_key</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">your-api-key</span><span class="sh">"</span><span class="p">,</span>
            <span class="sh">"</span><span class="s">model</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">text-embedding-3-small</span><span class="sh">"</span>
        <span class="p">}</span>
    <span class="p">},</span>
    <span class="sh">"</span><span class="s">graph_store</span><span class="sh">"</span><span class="p">:</span> <span class="p">{</span>
        <span class="sh">"</span><span class="s">provider</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">neo4j</span><span class="sh">"</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">config</span><span class="sh">"</span><span class="p">:</span> <span class="p">{</span>
            <span class="sh">"</span><span class="s">url</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">neo4j+s://your-instance</span><span class="sh">"</span><span class="p">,</span>
            <span class="sh">"</span><span class="s">username</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">neo4j</span><span class="sh">"</span><span class="p">,</span>
            <span class="sh">"</span><span class="s">password</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">password</span><span class="sh">"</span>
        <span class="p">}</span>
    <span class="p">},</span>
    <span class="sh">"</span><span class="s">history_db_path</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">/path/to/history.db</span><span class="sh">"</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">version</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">v1.1</span><span class="sh">"</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">custom_fact_extraction_prompt</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">Optional custom prompt for fact extraction for memory</span><span class="sh">"</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">custom_update_memory_prompt</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">Optional custom prompt for update memory</span><span class="sh">"</span>
<span class="p">}</span>
</code></pre></div></div> <ul> <li>history_db_path：Mem0 每次生成新记忆时，不仅会保存在向量数据库里，而且还在本地数据库中留有一份副本。这个本地数据库使用的是 SQLite。</li> <li>version：目前支持 v1.0 和 v1.1 两个版本，默认使用的是最新的 v1.1 版本。</li> <li>custom_fact_extraction_prompt：事实提取的 Prompt。</li> <li>custom_update_memory_prompt：事实更新的 Prompt，默认使用 Mem0 提供的 Prompt。</li> </ul> <p>还有一些系统提示词是系统定义好的：</p> <ul> <li>DEFAULT_UPDATE_MEMORY_PROMPT</li> <li>USER_MEMORY_EXTRACTION_PROMPT</li> <li>AGENT_MEMORY_EXTRACTION_PROMPT</li> </ul> <h2 id="default_update_memory_prompt">DEFAULT_UPDATE_MEMORY_PROMPT</h2> <p>在 Prompt 中找到 DEFAULT_UPDATE_MEMORY_PROMPT 简单翻译一下：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>DEFAULT_UPDATE_MEMORY_PROMPT = """你是一个智能记忆管理器，负责控制系统的记忆。
你可以执行四种操作：(1) 添加到记忆中，(2) 更新记忆，(3) 从记忆中删除，以及 (4) 不做改变。

基于上述四种操作，记忆将会发生变化。

将新提取到的事实与现有记忆进行比较。对于每个新事实，决定是否：
- 添加（ADD）：将其作为新元素添加到记忆中
- 更新（UPDATE）：更新现有的记忆元素
- 删除（DELETE）：删除现有的记忆元素
- 无变化（NONE）：不做任何改变（如果该事实已存在或不相关）

以下是选择执行哪种操作的具体指南：

1. **添加**：如果提取到的事实包含记忆中不存在的新信息，那么你必须通过在id字段中生成一个新ID来添加它。

2. **更新**：如果提取到的事实包含已经存在于记忆中但信息完全不同的内容，那么你必须更新它。
如果提取到的事实包含与记忆中现有元素表达相同内容的信息，那么你必须保留信息量最大的事实。
示例(a) -- 如果记忆中包含"用户喜欢打板球"，而提取到的事实是"喜欢和朋友一起打板球"，那么用提取到的事实更新记忆。
示例(b) -- 如果记忆中包含"喜欢奶酪披萨"，而提取到的事实是"爱奶酪披萨"，那么你不需要更新它，因为它们表达的是相同的信息。
如果指示是更新记忆，那么你必须更新它。
请记住在更新时保持相同的ID。
请注意只从输入ID中返回输出中的ID，不要生成任何新ID。

3. **删除**：如果提取到的事实包含与记忆中现有信息相矛盾的信息，那么你必须删除它。或者如果指示是删除记忆，那么你必须删除它。
请注意只从输入ID中返回输出中的ID，不要生成任何新ID。

4. **无变化**：如果提取到的事实包含已经存在于记忆中的信息，那么你不需要做任何改变。

"""
</code></pre></div></div> <p>这段 Prompt 为每一种操作提供了对应示例，根据大模型返回的 event 类型，对旧记忆分别执行对应的操作：</p> <ul> <li>新增记忆 _create_memory：在向量数据库新增一条记忆，同时在历史库新增一条 ADD 记录；</li> <li>更新记忆 _update_memory：更新向量数据库中的已有记忆，同时在历史库新增一条 UPDATE 记录；</li> <li>删除记忆 _delete_memory：删除向量数据库中的已有记忆，同时在历史库新增一条 DELETE 记录；</li> </ul> <h2 id="记忆类型">记忆类型</h2> <p>在 mem0/mem0/configs/enums.py 路径下定义了三种记忆类型：</p> <table> <thead> <tr> <th style="text-align: left">维度</th> <th style="text-align: left">SEMANTIC (语义)</th> <th style="text-align: left">EPISODIC (情景)</th> <th style="text-align: left">PROCEDURAL (程序)</th> </tr> </thead> <tbody> <tr> <td style="text-align: left">核心问题</td> <td style="text-align: left">是什么？ (What)</td> <td style="text-align: left">何时/何地？ (When/Where)</td> <td style="text-align: left">怎么做？ (How)</td> </tr> <tr> <td style="text-align: left">内容性质</td> <td style="text-align: left">事实、知识、偏好、定义</td> <td style="text-align: left">事件、经历、对话片段</td> <td style="text-align: left">技能、流程、规则、习惯</td> </tr> <tr> <td style="text-align: left">时效性</td> <td style="text-align: left">长期稳定，不易随时间改变</td> <td style="text-align: left">短期或中期，随新事件更新</td> <td style="text-align: left">长期稳定，除非重新训练/更新规则</td> </tr> <tr> <td style="text-align: left">依赖上下文</td> <td style="text-align: left">低（独立于具体时间）</td> <td style="text-align: left">高（强依赖时间/地点）</td> <td style="text-align: left">中（依赖任务触发）</td> </tr> <tr> <td style="text-align: left">AI 检索示例</td> <td style="text-align: left">“用户喜欢红色”</td> <td style="text-align: left">“用户昨天买了红色的车”</td> <td style="text-align: left">“购车流程是先选颜色再付款”</td> </tr> <tr> <td style="text-align: left">数据库策略</td> <td style="text-align: left">需去重、合并相似事实</td> <td style="text-align: left">按时间排序、保留原始细节</td> <td style="text-align: left">结构化存储、作为 System Prompt</td> </tr> </tbody> </table> <p>当用户向 Mem0 添加一条记忆时（memory.add(…)），框架内部会自动分析这条文本属于哪种类型，并打上标签：</p> <ol> <li>输入：“我每天早上 8 点跑步。” <ul> <li>LLM 判断：这既是一个事实（语义：用户有晨跑习惯），也是一个重复发生的事件（情景），甚至隐含了一种生活规则（程序：保持健康的生活方式）。</li> <li>Mem0 处理：可能会将其主要归类为 SEMANTIC（作为用户画像），或者如果强调了“今天早上”，则归为 EPISODIC。</li> </ul> </li> <li>输入：“每次处理退款请求时，必须先检查订单状态。” <ul> <li>LLM 判断：这是一个操作规则。</li> <li>Mem0 处理：归类为 PROCEDURAL。当 Agent 下次遇到退款请求时，会优先检索此类记忆来指导行动。</li> </ul> </li> </ol>]]></content><author><name></name></author><category term="技术"/><category term="AI"/><summary type="html"><![CDATA[从短期到长期的技术架构与时间]]></summary></entry><entry><title type="html">LangGraph 学习</title><link href="https://xuyuanzhe.github.io/blog/2025/langgraph/" rel="alternate" type="text/html" title="LangGraph 学习"/><published>2025-11-16T15:00:00+00:00</published><updated>2025-11-16T15:00:00+00:00</updated><id>https://xuyuanzhe.github.io/blog/2025/langgraph</id><content type="html" xml:base="https://xuyuanzhe.github.io/blog/2025/langgraph/"><![CDATA[<h1 id="是什么">是什么？</h1> <p>LangGraph 是 LangChain 生态的底层工作流编排框架，用有向图结构管理复杂 AI Agent 系统</p> <h1 id="有什么优势">有什么优势？</h1> <ul> <li>持久执行：支持故障恢复，自动从断点继续执行，适合长期运行任务</li> <li>可视化流程：与 LangSmith 集成，提供执行路径追踪和状态转换可视化</li> <li>动态控制：条件分支、循环、并行执行，提供细粒度控制，开发者可精确设计和观察每一步行为</li> <li>状态管理：内置状态持久化，支持短期和长期记忆，保持上下文一致性</li> <li>人机协作：无缝集成人工干预，可在任意执行点检查和修改状态</li> </ul> <h1 id="核心组件">核心组件</h1> <p>langgraph 的核心是有向图模型，由三个基本组件构成</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>节点(Node) → 边(Edge) → 状态(State)
</code></pre></div></div> <ul> <li>节点 (Node)：工作流中的基本处理单元，可代表 LLM 调用、工具执行或自定义逻辑，接收并处理状态，返回更新后的状态</li> <li>边 (Edge)：定义节点间的连接关系和执行路径，分为： <ul> <li>普通边：固定执行顺序</li> <li>条件边：根据状态值决定下一步路径，实现动态决策</li> </ul> </li> <li>状态 (State)：贯穿整个工作流的数据结构，在节点间流动并被修改，支持短期工作记忆和长期持久化存储</li> </ul> <h1 id="安装">安装</h1> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 必要依赖</span>
pip <span class="nb">install </span>langchain langgraph langchain-community

<span class="c"># 可视化工具</span>
pip <span class="nb">install </span>langsmith
</code></pre></div></div> <h1 id="demo">Demo</h1> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">typing</span> <span class="kn">import</span> <span class="n">List</span><span class="p">,</span> <span class="n">Dict</span><span class="p">,</span> <span class="n">Tuple</span>
<span class="kn">from</span> <span class="n">pydantic</span> <span class="kn">import</span> <span class="n">BaseModel</span>
<span class="kn">from</span> <span class="n">langgraph.graph</span> <span class="kn">import</span> <span class="n">StateGraph</span><span class="p">,</span> <span class="n">END</span>
<span class="kn">from</span> <span class="n">langchain_core.language_models</span> <span class="kn">import</span> <span class="n">ChatOpenAI</span>

<span class="c1"># 1. 定义状态
</span><span class="k">class</span> <span class="nc">ChatState</span><span class="p">(</span><span class="n">BaseModel</span><span class="p">):</span>
    <span class="n">messages</span><span class="p">:</span> <span class="n">List</span><span class="p">[</span><span class="n">Dict</span><span class="p">[</span><span class="nb">str</span><span class="p">,</span> <span class="nb">str</span><span class="p">]]</span> <span class="o">=</span> <span class="p">[]</span>
    <span class="n">current_input</span><span class="p">:</span> <span class="nb">str</span> <span class="o">=</span> <span class="sh">""</span>
    <span class="n">should_continue</span><span class="p">:</span> <span class="nb">bool</span> <span class="o">=</span> <span class="bp">True</span>

<span class="c1"># 2. 定义节点函数
</span><span class="k">async</span> <span class="k">def</span> <span class="nf">process_user_input</span><span class="p">(</span><span class="n">state</span><span class="p">:</span> <span class="n">ChatState</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">ChatState</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">处理用户输入</span><span class="sh">"""</span>
    <span class="n">messages</span> <span class="o">=</span> <span class="n">state</span><span class="p">.</span><span class="n">messages</span> <span class="o">+</span> <span class="p">[{</span><span class="sh">"</span><span class="s">role</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">user</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">content</span><span class="sh">"</span><span class="p">:</span> <span class="n">state</span><span class="p">.</span><span class="n">current_input</span><span class="p">}]</span>
    <span class="k">return</span> <span class="nc">ChatState</span><span class="p">(</span>
        <span class="n">messages</span><span class="o">=</span><span class="n">messages</span><span class="p">,</span>
        <span class="n">current_input</span><span class="o">=</span><span class="n">state</span><span class="p">.</span><span class="n">current_input</span><span class="p">,</span>
        <span class="n">should_continue</span><span class="o">=</span><span class="bp">True</span>
    <span class="p">)</span>

<span class="k">async</span> <span class="k">def</span> <span class="nf">generate_ai_response</span><span class="p">(</span><span class="n">state</span><span class="p">:</span> <span class="n">ChatState</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">ChatState</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">生成AI回复</span><span class="sh">"""</span>
    <span class="n">llm</span> <span class="o">=</span> <span class="nc">ChatOpenAI</span><span class="p">(</span><span class="n">temperature</span><span class="o">=</span><span class="mf">0.7</span><span class="p">)</span>
    <span class="n">response</span> <span class="o">=</span> <span class="k">await</span> <span class="n">llm</span><span class="p">.</span><span class="nf">ainvoke</span><span class="p">(</span><span class="n">state</span><span class="p">.</span><span class="n">messages</span><span class="p">)</span>
    <span class="n">messages</span> <span class="o">=</span> <span class="n">state</span><span class="p">.</span><span class="n">messages</span> <span class="o">+</span> <span class="p">[{</span><span class="sh">"</span><span class="s">role</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">assistant</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">content</span><span class="sh">"</span><span class="p">:</span> <span class="n">response</span><span class="p">}]</span>
    <span class="k">return</span> <span class="nc">ChatState</span><span class="p">(</span>
        <span class="n">messages</span><span class="o">=</span><span class="n">messages</span><span class="p">,</span>
        <span class="n">current_input</span><span class="o">=</span><span class="n">state</span><span class="p">.</span><span class="n">current_input</span><span class="p">,</span>
        <span class="n">should_continue</span><span class="o">=</span><span class="bp">True</span>
    <span class="p">)</span>

<span class="k">def</span> <span class="nf">should_continue</span><span class="p">(</span><span class="n">state</span><span class="p">:</span> <span class="n">ChatState</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">决定是否继续对话</span><span class="sh">"""</span>
    <span class="k">if</span> <span class="sh">"</span><span class="s">goodbye</span><span class="sh">"</span> <span class="ow">in</span> <span class="n">state</span><span class="p">.</span><span class="n">current_input</span><span class="p">.</span><span class="nf">lower</span><span class="p">():</span>
        <span class="k">return</span> <span class="sh">"</span><span class="s">end</span><span class="sh">"</span>
    <span class="k">return</span> <span class="sh">"</span><span class="s">continue</span><span class="sh">"</span>

<span class="c1"># 3. 构建图
</span><span class="n">workflow</span> <span class="o">=</span> <span class="nc">StateGraph</span><span class="p">(</span><span class="n">ChatState</span><span class="p">)</span>

<span class="c1"># 添加节点
</span><span class="n">workflow</span><span class="p">.</span><span class="nf">add_node</span><span class="p">(</span><span class="sh">"</span><span class="s">process_input</span><span class="sh">"</span><span class="p">,</span> <span class="n">process_user_input</span><span class="p">)</span>
<span class="n">workflow</span><span class="p">.</span><span class="nf">add_node</span><span class="p">(</span><span class="sh">"</span><span class="s">generate_response</span><span class="sh">"</span><span class="p">,</span> <span class="n">generate_ai_response</span><span class="p">)</span>

<span class="c1"># 添加边
</span><span class="n">workflow</span><span class="p">.</span><span class="nf">add_edge</span><span class="p">(</span><span class="sh">"</span><span class="s">process_input</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">generate_response</span><span class="sh">"</span><span class="p">)</span>
<span class="n">workflow</span><span class="p">.</span><span class="nf">add_conditional_edges</span><span class="p">(</span>
    <span class="sh">"</span><span class="s">generate_response</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">should_continue</span><span class="p">,</span>
    <span class="p">{</span>
        <span class="sh">"</span><span class="s">continue</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">process_input</span><span class="sh">"</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">end</span><span class="sh">"</span><span class="p">:</span> <span class="n">END</span>
    <span class="p">}</span>
<span class="p">)</span>

<span class="c1"># 4. 编译图
</span><span class="n">app</span> <span class="o">=</span> <span class="n">workflow</span><span class="p">.</span><span class="nf">compile</span><span class="p">()</span>

<span class="c1"># 5. 运行对话
</span><span class="k">async</span> <span class="k">def</span> <span class="nf">chat</span><span class="p">():</span>
    <span class="n">state</span> <span class="o">=</span> <span class="nc">ChatState</span><span class="p">()</span>
    <span class="k">while</span> <span class="bp">True</span><span class="p">:</span>
        <span class="n">user_input</span> <span class="o">=</span> <span class="nf">input</span><span class="p">(</span><span class="sh">"</span><span class="s">You: </span><span class="sh">"</span><span class="p">)</span>
        <span class="n">state</span><span class="p">.</span><span class="n">current_input</span> <span class="o">=</span> <span class="n">user_input</span>
        <span class="n">state</span> <span class="o">=</span> <span class="k">await</span> <span class="n">app</span><span class="p">.</span><span class="nf">ainvoke</span><span class="p">(</span><span class="n">state</span><span class="p">)</span>
        <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">Bot:</span><span class="sh">"</span><span class="p">,</span> <span class="n">state</span><span class="p">.</span><span class="n">messages</span><span class="p">[</span><span class="o">-</span><span class="mi">1</span><span class="p">][</span><span class="sh">"</span><span class="s">content</span><span class="sh">"</span><span class="p">])</span>
        <span class="k">if</span> <span class="ow">not</span> <span class="n">state</span><span class="p">.</span><span class="n">should_continue</span><span class="p">:</span>
            <span class="k">break</span>

<span class="c1"># 运行聊天
</span><span class="kn">import</span> <span class="n">asyncio</span>
<span class="n">asyncio</span><span class="p">.</span><span class="nf">run</span><span class="p">(</span><span class="nf">chat</span><span class="p">())</span>
</code></pre></div></div>]]></content><author><name></name></author><category term="技术"/><category term="AI"/><summary type="html"><![CDATA[多智能体]]></summary></entry><entry><title type="html">初识多智能体</title><link href="https://xuyuanzhe.github.io/blog/2025/multi-agent/" rel="alternate" type="text/html" title="初识多智能体"/><published>2025-11-10T15:00:00+00:00</published><updated>2025-11-10T15:00:00+00:00</updated><id>https://xuyuanzhe.github.io/blog/2025/multi-agent</id><content type="html" xml:base="https://xuyuanzhe.github.io/blog/2025/multi-agent/"><![CDATA[<h1 id="p1-什么是多智能体系统">P1 什么是多智能体系统？</h1> <p>一个多智能体系统是由多个<strong>能够自主行动的智能体（Agent）组成的计算系统</strong>。 这些智能体在一个共享的环境中进行交互，彼此之间可以通信、协作、竞争或协商，从而解决单个智能体难以或无法解决的复杂问题。</p> <p>我一开始偏颇的认为，所谓的 Multi Agent System 不过是 prompt engineering，给不同的 Agent 设置不同的角色扮演就变成了多智能体系统。 实则不然，高级的 Prompt Engineering 只是构建多智能体系统的一个<strong>必要条件</strong>。比如：</p> <ul> <li>角色设定： “你是一个项目总监 Agent…”</li> <li>流程定义： “你的工作流程是规划、委派、整合…”</li> </ul> <p>但是，一个真正健壮、高效的多智能体系统，还包含了远超 Prompt Engineering 的“系统工程”设计： 比如系统架构设计。用编排者-工作者（orchestrator-worker）模式，还是评估器-优化器（Evaluator-optimizer）（一些 Agent 负责生成结果，另一些 Agent 负责评估和反馈）模式， 还是更复杂的层级结构？这需要根据任务类型进行权衡，是软件架构层面的思考。再比如工具设计与开发，单个 Agent 主要由四个关键组件构成，Agent 使用的工具（API，MCP Tool）对性能有显著影响。 例如，工具是否能被 Agent 正确理解，工具被正确调用率如何？除此之外还有状态管理（持久化 memory 与异常恢复），成本控制等等。</p> <p><img src="/assets/img/posts/2025-11-10_09-56-03.jpg" class="card-img-top" width="100%" height="auto" alt="project thumbnail" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/></p> <blockquote> <h5 id="note">Note</h5> <p>我们对于多智能体系统的认知，有时会陷入与对“管理”相似的误区。</p> <p>外行视角下的管理，似乎只是开会与分配任务，容易产生“其本质不过是上传下达”的简化认知。同样，对多智能体系统的初步理解，也可能停留在“将一个大 Prompt 拆分为几个小 Prompt”的表层操作上。</p> <p class="block-tip">然而，无论是管理人类团队还是构建多智能体系统，核心挑战都是从“依赖个体超级能力”转向“设计一个能让平凡个体创造超级结果的系统”。</p> </blockquote> <h1 id="p2-我们为什么需要多智能体系统">P2 我们为什么需要多智能体系统？</h1> <p>一个更深刻的问题是：我们为什么非要走这条更复杂的路？ 一个更强大的单智能体，比如未来的 Gemini 3，Claude 5 或 GPT-6，难道不能解决所有问题吗？</p> <p>Anthropic 的答案是否定的。他们用实践证明，多智能体系统解决的是单智能体在结构上就难以克服的根本性问题。</p> <p>现实世界中真正有价值的任务，比如进行一项前沿科技的市场调研，或为一家公司制定未来三年的增长战略——其本质是<strong>开放式的</strong>和<strong>不可预测的</strong>。 你无法在开始时就用一个固定的流程图来规划所有步骤。恰恰相反，解决这类问题的过程本身就是一个不断发现新线索、调整方向、甚至彻底改变初始假设的动态过程。</p> <p>困境：单智能体的“隧道视野”</p> <p>先不考虑多智能体的并行执行带来的高效，一个单智能体就像一个独自探案的侦探，它沿着一条线索深入挖掘，可能会因为过于专注而错过其他同样重要的线索， 难以在深入细节的同时保持对全局的“上帝视角“般的观察。它的思考是线性的，应对一些“意外情况”非常脆弱。比如一旦初始路径有误，它的调整能力非常有限， 因为它只有一个“大脑”和一个“上下文”。用 Anthropic 的话说，“A linear, one-shot pipeline cannot handle these tasks”。</p> <p>优势：多智能体的“上帝视角”</p> <p><img src="/assets/img/posts/2025-11-10_10-06-58.jpg" class="card-img-top" width="100%" height="auto" alt="project thumbnail" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/></p> <p>我们以编排者-工作者（orchestrator-worker）模式为例，Anthropic 的 Research 功能便是基于此类模式，如上图所示，多智能体系统则从结构上天然解决了单智能体的困境。 它引入了两个核心概念：并行探索和关注点分离。</p> <ul> <li>并行探索： 主智能体（Orchestrator）如同迷宫入口的总指挥，派遣多个子智能体同时探索不同的路径。这确保了在任何时刻，系统都在进行广度优先的探索，极大地增加了发现“最优解”的概率。同时，依据概率论的先验，可以用类似投票的方法保证结果的稳定性，减少方差。</li> <li>关注点分离： 每个子智能体都是一个独立的“专家”，拥有自己独立的上下文窗口、工具集和探索历史。一个负责技术分析的 Agent 不会被市场新闻所“污染”。这种隔离保证了每个子任务的深度和纯粹性。</li> </ul> <p>正如 Anthropic 强调的：“你无法硬编码一个探索复杂主题的固定路径，因为这个过程是内在动态和路径依赖的。” 多智能体系统正是为了拥抱这种动态性而生的。</p> <p>在过去的一段时间，我们听到的更多情况下是描述预训练领域，即通过更大的参数/更多的数据来让基础模型更聪明。但目前的一些实验结果和研究在逐步表明：在达到一定智能阈值后，性能的决定性因素，变成了你能否为问题投入足够多的“有效算力”，即 Token。</p> <p>Multi Agent System 天然可以承载更多的 token 消耗，因为多智能体系统多 agent 的结构决定了它几乎无上限的 token 容量。</p> <p>在 Anthropic 的实践中，他们发现三个因素解释了 BrowseComp[2] 评估（测试浏览智能体定位难找信息的能力）中 95% 的性能差异。其中 Token 使用量本身解释了 80% 的差异，而工具调用次数和模型选择是另外两个解释性因素。这一发现验证了 Anthropic 的多智能体架构，该架构将工作分配给具有独立上下文窗口的智能体，以增加并行推理的能力。</p> <p>这正是多智能体系统发挥其魔力的关键点。单智能体受限于其一次性的上下文窗口大小和处理能力，其“Token 预算”存在物理上限。而多智能体系统，通过将一个大任务分解成多个子任务，并分配给拥有独立上下文窗口的多个子智能体来执行，本质上是一种极其高效地扩展总 Token 使用量的架构（几乎无上限的 token）。</p> <p>同时，这些架构也存在成本上的问题。在实践中，这些架构消耗 Token 的速度很快。基于 Anthropic 的数据，智能体通常比聊天交互（chatbot）多使用约 4 倍的 Token，而多智能体系统比聊天多使用约 15 倍的 Token。为了在经济上可行，多智能体系统需要用于那些任务价值足够高，能够支付得起性能提升带来的成本的任务。</p> <blockquote> <h5 id="tip">TIP</h5> <p>我认为这个优势足够有价值。因为它可能开辟新的赛道，就目前而言大部分厂商在基模训练这块已经失去参赛资格。</p> <p class="block-tip">但是，多智能体系统可能会开创新的赛道，从单纯追求基模的硬智商到建立系统化/工程化的多智能体系统。基于非 SOTA 模型在某些特定领域取得领先。</p> </blockquote> <p>Anthropic 还提到了一个很有意思的观点便是：“搜索即智能，从庞大的语料库中提取见解”。</p> <p>在多智能体系统中，子智能体扮演了“智能过滤器”的角色。它们在各自的领域内并行工作，消化大量原始信息，然后将最关键的“压缩后”的见解（the most important tokens）提交给主智能体。</p> <p>这种机制极大地减轻了主智能体（Lead Agent）的认知负担。它无需亲自阅读每一份原始文档，只需处理由各个专家团队提交上来的、经过预处理和提炼的“摘要报告”。这使得主智能体可以专注于更高层次的战略规划、逻辑推理和最终决策，从而产生更高质量的综合性输出。</p> <p>综上所述，多智能体系统为应对复杂性问题提供了一个有效的架构选择。它通过模拟协作模式，旨在突破单体智能固有的局限；通过架构创新，提升了系统在推理上可投入的“算力预算”上限；并通过分层信息处理，实现了高效的洞察提炼。</p> <h1 id="p3-构建多智能体系统的最佳实践是什么">P3 构建多智能体系统的最佳实践是什么？</h1> <h2 id="31-anthropic-architecture-overview-for-research">3.1 Anthropic: Architecture overview for Research</h2> <p>Anthropic 在博客中的例子是他们如何设计 Claude Research 功能的。Research 或者说 Deep Research 是近期的潮流，这个任务本质上是检索大量信息， 从中提炼有价值的内容形成一个总结，在任何领域都有用武之地。</p> <p>在深入其架构细节之前，我推荐大家简单思考下基于 Multi Agent System 的 Deep Research 与 RAG（检索增强生成）的不同。 传统 RAG 采用的是静态检索：一次性地根据用户查询抓取最相关的文本块，然后基于这些固定的信息生成答案。</p> <p>而基于 Multi Agent System 的 Deep Research 则是一种动态、多步的研究过程。它并非一次性“获取”信息（搜索 + 重排），而是通过智能体的主动探索去“生成”洞察。并根据研究途中的新发现，实时调整策略。</p> <p>Anthropic 采用的是经典的编排者-工作者（Orchestrator-Worker）模式：</p> <p><img src="/assets/img/posts/2025-11-10_10-12-07.jpg" class="card-img-top" width="100%" height="auto" alt="project thumbnail" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/></p> <p>上图完整展示了其工作流程。当用户提交一个查询时，系统的工作流如下：</p> <ol> <li>启动与规划： 系统创建一个主研究员智能体 (Lead Researcher)。它首先思考并制定一个研究计划，并将这个计划保存到记忆 (Memory) 中。这是至关重要的一步，因为它确保了在长流程任务中，即使上下文窗口被截断，核心目标和计划也不会丢失。</li> <li>委派与并行： 主智能体根据计划，创建多个专门的子智能体 (Subagents)，并为每个子智能体分配具体的、独立的子任务。（在图中，创建了两个 sub-agents）</li> <li>执行与评估 (引入“交错思考”)： 每个子智能体独立地执行任务（如网络搜索）。关键在于，它们使用 “交错思考” (Interleaved Thinking) 的技术。这意味着，在两次工具调用之间，Agent 会插入一个“思考步骤”，用于评估上一步工具返回的结果质量，识别信息差距，并迭代优化自己的下一步行动</li> <li>综合与决策： 子智能体将它们的发现返回给主智能体（压缩）。主智能体综合这些结果，并判断是否需要进一步的研究。如果需要，它可以创建更多的子智能体或调整策略。</li> <li>引用与输出： 一旦收集到足够的信息，系统会调用一个专门的引用智能体 (CitationAgent)，它负责处理所有文档和研究报告，确保所有声明都有据可查。最终，一份带有精确引用的完整研究结果将返回给用户。</li> </ol> <h2 id="32-更多多智能体系统构建的实践">3.2 更多多智能体系统构建的实践</h2> <p>多智能体系统与单智能体系统有关键区别，其中包括协调复杂性的迅速增长。智能体开发的早期容易犯一些错误，比如为简单查询生成 50 个子智能体，无休止地在网上搜索不存在的来源，以及智能体之间通过过多的更新互相干扰。虽然，上文中提到多智能体系统不等价于 Prompt Engineering 但由于每个智能体都由 Prompt 引导，Prompt Engineering 是我们改进多智能体系统行为的主要手段。Anthropic 基于他们的经验总结了 8 条最有价值的 Agent Prompt Engineering 实践。</p> <p>我基于个人理解分为了三类，分别是：管理层面；执行层面；细节层面。做了个简单示意图如下：</p> <p><img src="/assets/img/posts/2025-11-10_10-14-22.jpg" class="card-img-top" width="100%" height="auto" alt="project thumbnail" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/></p> <p><strong>管理层面</strong></p> <p>这部分主要偏向主智能体（Orchestrator）需要的管理哲学。它关乎方向、资源和目标。</p> <ul> <li>学会有效授权 (Teach the orchestrator how to delegate) 将模糊任务清晰化。为子智能体提供明确的目标、格式、工具和边界，这是项目成功的基石。避免让 Lead Agent 给出诸如 “研究半导体短缺” 模糊不清的指令，这可能会导致 Sub Agent 做重复的工作。Anthropic 最初允许允许 Orchestrator 给出简单的、简短的指令，但发现这些指令常常模糊不清，导致 Sub-agent 误解任务或执行与其他智能体完全相同的搜索</li> <li>合理分配资源 (Scale effort to query complexity) 其实相当于在 prompt 中 hardcode 一些先验。防止在简单问题上“杀鸡用牛刀”。简单的事实查找通常只需要一个 Agent 进行 3 到 10 次工具调用等等，诸如此类的先验知识。</li> </ul> <p><strong>执行层面</strong></p> <p>这部分主要偏向子智能体（Sub-agent）在执行任务时需要遵循的具体策略。它关乎效率、质量和速度。</p> <ul> <li>先广后精的探索策略 (Start wide, then narrow down) 搜索策略应模仿专家级的人类研究：在深入具体细节之前先探索整体概况。智能体通常默认使用过长、过于具体的查询，这会导致返回结果很少。Anthropic 发现通过提示智能体从简短、宽泛的查询开始，评估可用的信息，然后逐步缩小焦点，可以有效纠正这种倾向。</li> <li>引导思考过程 (Guide the thinking process) 这条现在比较常见了，在 Claude 中对应扩展思考（Extend Thinking）使用扩展思考可以为智能体后续的行为提供“草稿纸”。有助于智能体的规划，设计。同时引入上文中提到的“交错思考[3]”，让智能体在工具调用之间能够进行思考，从而可以动态调整。•</li> <li>并行工具调用优化速度和性能 (Parallel tool calling transforms speed) 复杂的研究任务（DeepResearch）自然涉及探索许多来源。Anthropic 在早期的智能体执行时使用顺序搜索，速度慢得令人痛苦。为了提速，引入了两种并行化：（1）主智能体并行启动 3-5 个子智能体，而不是串行启动；（2）子智能体并行使用 3 个以上的工具。这些改变使复杂查询的研究时间减少了高达 90%，让研究功能在几分钟内完成更多工作，而不是几小时，同时覆盖的信息比其他系统更多。更进一步总结就是：在任务级和工具级同时实现并行化。</li> </ul> <p><strong>细节层面</strong></p> <p>这部分更多的是指导在构建多智能体系统的细节部分。它关乎如何调试 prompt，如何设计与选择工具等。</p> <ul> <li>像你的智能体一样思考 (Think like your agents) 要迭代 prompt，必须理解它的效果。类似于单步调试，逐步观察智能体的工作。这样有助于你发现智能体是在哪些环节出问题的，例如“智能体在已有足够结果时仍继续工作，使用过于冗长的搜索查询，或选择不正确的工具”。</li> <li>工具设计与选择至关重要 (Tool design and selection are critical) 工具之于 LLM 有点像 UI 之于人类。UI 的全称是 User Interface，面向用户的接口。所以，工具例如 MCP 等就是面向 LLM 的接口。使用正确的工具是高效的乃至必要的。糟糕的工具描述会使智能体走上完全错误的道路，所以每个工具都需要一个明确的目的和清晰的描述。同时，Anthropic 还设计明确的启发式规则：首先检查所有可用工具，将工具使用与用户意图匹配，进行广泛的外部探索时搜索网络，或优先选择专用工具而非通用工具（examine all available tools first, match tool usage to user intent, search the web for broad external exploration, or prefer specialized tools over generic ones）</li> <li>赋予智能体自我进化的能力 (Let agents improve themselves) 当大模型自己优化自己，如同我们在 Claude Code / Cursor 等 AI Coding 软件把它们写的 bug 让它们自己解决。Claude4 等模型也可以是出色的提示工程师。当给定一个 Prompt 和一种失败场景时，它们能够诊断出智能体失败的原因并提出改进建议。甚至，你可以尝试开发一些工具测试 Agent，来自动优化一些 MCP 工具。</li> </ul> <p>大部分的 tips 还需要大家结合自己的实践去进一步理解。 总的来说，智能体的 prompt 应该侧重于灌输良好的启发式方法，而非 hardcode 的规则（这样就变得有点 if-else 的死板）。Anthropic 认为： 除了上述的 8 个建议，你需要深入你所要做的领域，比如当你做 Deep Research 时你需要思考专业的人是如何进行研究任务的？ 总结后启发式的融入我们的 prompt 中，比如将大的研究问题分解成小问题，以及识别何时应该注重深度与广度。然后，通过设定一些明确的边界避免多智能体系统完全失控。 在做到上述两步后，构建一个可观测的测试用例反复迭代。这就是多智能体系统开发的最佳实践。</p> <p>无论是 agent 还是 multi agent system，构建原型都远比构建一个可以长期运行，不需要太多人工介入的 pipeline 简单的多。 所以，在构建好原型后，如何走完从原型到产品的这最后一公里呢？主要分为两大挑战：科学的评估与可靠的工程化。</p> <h4 id="第一步是评估如何对-agent-进行有效的评估">第一步是评估，如何对 agent 进行有效的评估？</h4> <p>agent 和传统的数据结构算法或者业务代码不同，传统的软件执行通常有明确的大致（多线程/多进程可能没那么固定）可预期的步骤，比如：给定输入 X，系统应遵循路径 Y 产生输出 Z。 但是 agent 尤其是 multi agent system 并非如此。即使起点相同，它们可能会采取完全不同的有效路径来达到目标。它们可能使用不同的工具，检索不同的来源。 因为，通常情况下解决问题并不只有一条正确路径，因此仅检查 agent 是否符合我们预设的所谓“正确的路径”不够严谨。我们需要更先进的评估方法，兼顾结果的检查和过程的检查。</p> <p><img src="/assets/img/posts/2025-11-10_10-18-19.jpg" class="card-img-top" width="100%" height="auto" alt="project thumbnail" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/></p> <p>立即用小样本开始评估。在 agent 开发的早期阶段，任何改变往往会产生巨大影响，因为有大量唾手可得的改进空间。一个 prompt 的调整可能会将成功率从 30% 提高到 80%。改进幅度这么明显，仅用几个测试用例就能发现变化。 Anthropic 团队从大约 20 个代表真实使用模式的查询集开始。测试这些查询通常能让我们清楚地看到变化的影响。经常有 AI 开发者团队推迟创建评估数据集，因为他们认为只有包含数百个测试用例的大型评估集才有用。然而，最好是立即用几个例子进行小规模测试，而不是等到能够构建更全面的评估时再开始。</p> <p>在合适时使用 LLM 作为评估者进行扩展。 如上文所说，在 Cursor 等 AI Coding 中使用 AI 解决 AI 生产的 Bug。在恰当的时候利用 AI 评估 AI。 我们需要对结果（输出）制定正确的评分标准，例如：事实准确性（声明是否与来源匹配？）、引用准确性（引用的来源是否与声明匹配？）、完整性（是否涵盖了所有被要求的内容？）、来源质量（是否使用了主要来源而非质量较低的次要来源？），以及工具效率（是否以合理的次数使用了正确的工具？）并把它合理的嵌入到 Prompt 中。 我们可以使用多个 LLM 并行的对不同部分进行评分（每个 LLM 负责一个指标），但是实验结果证明单个 LLM 调用，即使用单个 Prompt 输出一个评分结果是最统一的，并且和人类的判断最接近。</p> <p>必要时引入人工介入。 有时候自动化评估会遗漏一些边缘情况。例如，Anthropic 在实验中发现，早期 agent 在搜索时，倾向于选择 SEO 优化过的内容而非类似学术 PDF和个人博客等更权威但排名较低的来源。 因此，他们手动的在 prompt 中加入了启发式的规则解决了这个问题。这说明，尽管我们都期望能自动化所有内容，但是人工介入在某些情况下仍是必要的。</p> <p>多智能体系统很容易引发蝴蝶效应，对智能体的微小改动可能会不可预测的改变整个系统的走向。因此，构建成功的产品时，除了对单个 agent 行为的了解和分析外更需要你理解整个系统的交互模式。 因此，prompt 的最佳实践应该是启发式的，而非严格的 hardcode。应该关注的是如何管理划分问题，如何解决问题等等。过于严格的 hardcode 只会变成 if-else 式的工作流。 做到这点并不容易，需要仔细的设计 prompt / 工具，应用启发式的方法以及完整的评估反馈体系。</p> <h4 id="第二步是生产化工程化如何构建稳定可靠的产品级-agentic-system-是当前时代的新挑战">第二步是生产化/工程化，如何构建稳定可靠的产品级 agentic system 是当前时代的新挑战。</h4> <p>多智能体系统很容易引发蝴蝶效应。在传统软件中，一个 bug 可能会破坏一个功能、降低性能或导致服务中断。在 agentic system 中，微小的变化会级联成巨大的行为变化，这使得为必须在长期运行过程中维持状态的复杂智能体编写代码变得异常困难。</p> <p>Anthropic 分享了他们在生产化中遇到的四大挑战与对策：</p> <ul> <li>状态与错误 (Stateful &amp; Errors): Agent 是长期运行、有状态的。一个微小的系统失败都可能对 Agent 造成灾难性影响，而重启的代价（Token 消耗、用户体验）是昂贵的。对策是构建能从断点恢复的系统，并结合模型的智能（如告知 Agent 工具失效，让其自行适应）和工程的确定性（如重试逻辑、检查点）来优雅地处理错误。</li> <li>调试 (Debugging): Agent 的非确定性让复现 Bug 难于登天。对策是唯一的出路——全面的生产链路追踪 (Full Production Tracing)。 必须记录下 Agent 的每一步决策、每一次工具调用和返回结果，才能系统性地诊断失败根源。</li> <li>部署 (Deployment): 如何在不中断正在运行的 Agent 的前提下，更新这个持续运行的复杂系统？对策是采用彩虹部署 (Rainbow Deployments)，让新旧版本的系统并行运行，逐步将流量从旧版本切换到新版本。</li> <li>瓶颈 (Bottlenecks): 当前的同步执行模式简化了协调，但也造成了瓶颈——整个系统被最慢的子 Agent 拖累。未来的方向是异步执行，虽然这会带来状态一致性、错误传递等新挑战，但其性能收益将是巨大的。</li> </ul> <p>对从业者而言除了“使用 AI”的能力之外，“组织 AI”的能力,构建 Agentic System 的能力正变得愈发关键。</p>]]></content><author><name></name></author><category term="技术"/><category term="AI"/><summary type="html"><![CDATA[多智能体]]></summary></entry></feed>